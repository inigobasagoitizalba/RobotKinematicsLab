package com.robotkinematicslab.mobile.ml.data

/** Encoder metadata used by the application feature registry. */
internal object FeatureDefinitionData {
    val classification:List<FeatureDefinition> by lazy {
        buildList {
            addAll(block0())
            addAll(block1())
            addAll(block2())
            addAll(block3())
            addAll(block4())
            addAll(block5())
            addAll(block6())
            addAll(block7())
            addAll(block8())
            addAll(block9())
            addAll(block10())
            addAll(block11())
            addAll(block12())
            addAll(block13())
            addAll(block14())
            addAll(block15())
            addAll(block16())
            addAll(block17())
            addAll(block18())
            addAll(block19())
            addAll(block20())
            addAll(block21())
            addAll(block22())
            addAll(block23())
            addAll(block24())
            addAll(block25())
            addAll(block26())
            addAll(block27())
            addAll(block28())
            addAll(block29())
            addAll(block30())
            addAll(block31())
            addAll(block32())
            addAll(block33())
            addAll(block34())
            addAll(block35())
            addAll(block36())
            addAll(block37())
            addAll(block38())
        }.also { FeatureDefinitionIntegrity.validate(ScientificDatasetTrainingReader.featureNames(TrainingFeatureProfile.CONTEXT_RESEARCH_V2),it) }
    }
    fun forEntry(entry:RegisteredFeatureSet):List<FeatureDefinition> {
        val byName=classification.associateBy { it.technicalId }
        return entry.featureNames.mapIndexed { offset,id ->
            val source=requireNotNull(byName[id]) { "No verified dictionary card for $id; review the changed encoder." }
            if(entry.domain==FeatureSetDomain.CLASSIFICATION) source.copy(index=offset+1)
            else source.copy(index=offset+1,
                origin="Verified IK encoder receives validated RobotDefinition, RobotState, target and IKConfig; no22-column stored classification-context block. The same ID can have a different local index or logarithm floor. Dependency column names below identify equivalent scientific CSV inputs, not direct columns consumed by this live encoder.",
                calculation=when(id) {
                    "log_ik_tolerance" -> "ln(config.tolerance); positive finite input, no1e-12 floor. Float conversion must remain finite."
                    "log_ik_damping" -> "ln(config.damping); positive finite input, no1e-12 floor. Float conversion must remain finite."
                    "log_ik_max_step" -> "ln(config.maxStep); positive finite input, no1e-12 floor. Float conversion must remain finite."
                    else -> source.calculation
                },
                limitations="IK LOCAL INDEX ${offset+1}; classification index ${source.index} is a different namespace. " +
                    if(id in setOf("log_ik_tolerance","log_ik_damping","log_ik_max_step")) "Unlike classification, this encoder applies natural log without flooring. IK model normalization is separate. Positive finite controls and finite Float outputs required."
                    else source.limitations.replace("Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.","Verified IK normalization is separate from this feature formula."),
                implementationSource="app/src/main/java/com/robotkinematicslab/mobile/ml/ik/OneMicronIkFeatureEncoder.kt:encode; shared formula: " + source.implementationSource)
        }.also { FeatureDefinitionIntegrity.validate(entry.featureNames,it) }
    }
    private fun block0():List<FeatureDefinition> = listOf(
        FeatureDefinition(1,
            "joint_count_ratio",
            "Joint count divided by ten",
            "Raw task and solver inputs",
            "Actual declared joint count as a fraction of the fixed ten-slot input capacity.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointCount / 10",
            "dimensionless",
            listOf("jointCount"),
            "Actual declared joint count as a fraction of the fixed ten-slot input capacity.",
            "Candidate input for testing whether joint count divided by ten helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1..10 supported; not an estimate of independent Cartesian DOFs. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:306–315 · encode",
            "feature-dictionary-v1"),
        FeatureDefinition(2,
            "target_x",
            "Target x coordinate",
            "Raw task and solver inputs",
            "Requested base-frame Cartesian x coordinate.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "targetX",
            "m",
            listOf("targetX"),
            "Requested base-frame Cartesian x coordinate.",
            "Candidate input for testing whether target x coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Position only; does not encode target orientation. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:306–315 · encode",
            "feature-dictionary-v1"),
        FeatureDefinition(3,
            "target_y",
            "Target y coordinate",
            "Raw task and solver inputs",
            "Requested base-frame Cartesian y coordinate.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "targetY",
            "m",
            listOf("targetY"),
            "Requested base-frame Cartesian y coordinate.",
            "Candidate input for testing whether target y coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Position only; does not encode target orientation. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:306–315 · encode",
            "feature-dictionary-v1"),
        FeatureDefinition(4,
            "target_z",
            "Target z coordinate",
            "Raw task and solver inputs",
            "Requested base-frame Cartesian z coordinate.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "targetZ",
            "m",
            listOf("targetZ"),
            "Requested base-frame Cartesian z coordinate.",
            "Candidate input for testing whether target z coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Position only; does not encode target orientation. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:306–315 · encode",
            "feature-dictionary-v1"),
        FeatureDefinition(5,
            "ik_iteration_budget_ratio",
            "IK iteration budget divided by ten thousand",
            "Raw task and solver inputs",
            "Requested positive solver iteration ceiling rescaled by a constant.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "ikMaxIterations / 10000.0",
            "dimensionless",
            listOf("ikMaxIterations"),
            "Requested positive solver iteration ceiling rescaled by a constant.",
            "Candidate input for testing whether ik iteration budget divided by ten thousand helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is the configured budget, not iterations actually consumed. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:306–315 · encode",
            "feature-dictionary-v1"),
        FeatureDefinition(6,
            "log_ik_tolerance",
            "Natural log of IK position tolerance",
            "Raw task and solver inputs",
            "Natural logarithm of the positive requested positional stopping tolerance.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "ln(max(ikToleranceMeters,1e-12))",
            "log of numeric metres",
            listOf("ikToleranceMeters"),
            "Natural logarithm of the positive requested positional stopping tolerance.",
            "Candidate input for testing whether natural log of ik position tolerance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "The classifier floors values below1e-12; Verified IK does not apply this floor. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:306–315 · encode",
            "feature-dictionary-v1"),
        FeatureDefinition(7,
            "log_ik_damping",
            "Natural log of requested IK damping",
            "Raw task and solver inputs",
            "Natural logarithm of configured initial damping.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "ln(max(ikDamping,1e-12))",
            "log of solver damping numeric value",
            listOf("ikDamping"),
            "Natural logarithm of configured initial damping.",
            "Candidate input for testing whether natural log of requested ik damping helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Damping is a solver control, not a measured physical property; values below1e-12 are floored. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:306–315 · encode",
            "feature-dictionary-v1"),
        FeatureDefinition(8,
            "log_ik_max_step",
            "Natural log of maximum normalized IK step",
            "Raw task and solver inputs",
            "Natural logarithm of requested normalized step ceiling.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "ln(max(ikMaxStep,1e-12))",
            "log of dimensionless step control",
            listOf("ikMaxStep"),
            "Natural logarithm of requested normalized step ceiling.",
            "Candidate input for testing whether natural log of maximum normalized ik step helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Configured ceiling, not the achieved joint displacement; floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:306–315 · encode",
            "feature-dictionary-v1"),
        FeatureDefinition(9,
            "joint_1_present",
            "Joint 1 presence indicator",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 0 < jointCount else0; padded value0 when jointCount < 1",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 1 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(10,
            "joint_1_is_prismatic",
            "Joint 1 prismatic type indicator",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[0]==PRISMATIC else0; padded value0 when jointCount < 1",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 1 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(11,
            "joint_1_dh_theta",
            "Joint 1 static DH theta angle",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[0]; padded value0 when jointCount < 1",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 1 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(12,
            "joint_1_dh_d",
            "Joint 1 static DH d offset",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[0]; padded value0 when jointCount < 1",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 1 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1")
    )
    private fun block1():List<FeatureDefinition> = listOf(
        FeatureDefinition(13,
            "joint_1_dh_a",
            "Joint 1 DH a link distance",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[0]; padded value0 when jointCount < 1",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 1 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(14,
            "joint_1_dh_alpha",
            "Joint 1 DH alpha twist",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[0]; padded value0 when jointCount < 1",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 1 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(15,
            "joint_1_minimum",
            "Joint 1 lower joint limit",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[0]; padded value0 when jointCount < 1",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 1 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(16,
            "joint_1_maximum",
            "Joint 1 upper joint limit",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[0]; padded value0 when jointCount < 1",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 1 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(17,
            "joint_1_home",
            "Joint 1 declared home coordinate",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[0]; padded value0 when jointCount < 1",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 1 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(18,
            "joint_1_seed",
            "Joint 1 initial solver coordinate",
            "Raw joint definition",
            "Joint 1 (ordered standard-DH row 1): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[0]; padded value0 when jointCount < 1",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 1th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 1 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 1 joints exist, this slot is exactly0; presence is classification variable#9. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(19,
            "joint_2_present",
            "Joint 2 presence indicator",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 1 < jointCount else0; padded value0 when jointCount < 2",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 2 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(20,
            "joint_2_is_prismatic",
            "Joint 2 prismatic type indicator",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[1]==PRISMATIC else0; padded value0 when jointCount < 2",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 2 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(21,
            "joint_2_dh_theta",
            "Joint 2 static DH theta angle",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[1]; padded value0 when jointCount < 2",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 2 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(22,
            "joint_2_dh_d",
            "Joint 2 static DH d offset",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[1]; padded value0 when jointCount < 2",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 2 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(23,
            "joint_2_dh_a",
            "Joint 2 DH a link distance",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[1]; padded value0 when jointCount < 2",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 2 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(24,
            "joint_2_dh_alpha",
            "Joint 2 DH alpha twist",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[1]; padded value0 when jointCount < 2",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 2 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1")
    )
    private fun block2():List<FeatureDefinition> = listOf(
        FeatureDefinition(25,
            "joint_2_minimum",
            "Joint 2 lower joint limit",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[1]; padded value0 when jointCount < 2",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 2 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(26,
            "joint_2_maximum",
            "Joint 2 upper joint limit",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[1]; padded value0 when jointCount < 2",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 2 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(27,
            "joint_2_home",
            "Joint 2 declared home coordinate",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[1]; padded value0 when jointCount < 2",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 2 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(28,
            "joint_2_seed",
            "Joint 2 initial solver coordinate",
            "Raw joint definition",
            "Joint 2 (ordered standard-DH row 2): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[1]; padded value0 when jointCount < 2",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 2th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 2 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 2 joints exist, this slot is exactly0; presence is classification variable#19. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(29,
            "joint_3_present",
            "Joint 3 presence indicator",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 2 < jointCount else0; padded value0 when jointCount < 3",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 3 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(30,
            "joint_3_is_prismatic",
            "Joint 3 prismatic type indicator",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[2]==PRISMATIC else0; padded value0 when jointCount < 3",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 3 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(31,
            "joint_3_dh_theta",
            "Joint 3 static DH theta angle",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[2]; padded value0 when jointCount < 3",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 3 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(32,
            "joint_3_dh_d",
            "Joint 3 static DH d offset",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[2]; padded value0 when jointCount < 3",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 3 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(33,
            "joint_3_dh_a",
            "Joint 3 DH a link distance",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[2]; padded value0 when jointCount < 3",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 3 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(34,
            "joint_3_dh_alpha",
            "Joint 3 DH alpha twist",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[2]; padded value0 when jointCount < 3",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 3 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(35,
            "joint_3_minimum",
            "Joint 3 lower joint limit",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[2]; padded value0 when jointCount < 3",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 3 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(36,
            "joint_3_maximum",
            "Joint 3 upper joint limit",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[2]; padded value0 when jointCount < 3",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 3 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1")
    )
    private fun block3():List<FeatureDefinition> = listOf(
        FeatureDefinition(37,
            "joint_3_home",
            "Joint 3 declared home coordinate",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[2]; padded value0 when jointCount < 3",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 3 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(38,
            "joint_3_seed",
            "Joint 3 initial solver coordinate",
            "Raw joint definition",
            "Joint 3 (ordered standard-DH row 3): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[2]; padded value0 when jointCount < 3",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 3th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 3 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 3 joints exist, this slot is exactly0; presence is classification variable#29. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(39,
            "joint_4_present",
            "Joint 4 presence indicator",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 3 < jointCount else0; padded value0 when jointCount < 4",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 4 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(40,
            "joint_4_is_prismatic",
            "Joint 4 prismatic type indicator",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[3]==PRISMATIC else0; padded value0 when jointCount < 4",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 4 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(41,
            "joint_4_dh_theta",
            "Joint 4 static DH theta angle",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[3]; padded value0 when jointCount < 4",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 4 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(42,
            "joint_4_dh_d",
            "Joint 4 static DH d offset",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[3]; padded value0 when jointCount < 4",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 4 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(43,
            "joint_4_dh_a",
            "Joint 4 DH a link distance",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[3]; padded value0 when jointCount < 4",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 4 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(44,
            "joint_4_dh_alpha",
            "Joint 4 DH alpha twist",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[3]; padded value0 when jointCount < 4",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 4 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(45,
            "joint_4_minimum",
            "Joint 4 lower joint limit",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[3]; padded value0 when jointCount < 4",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 4 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(46,
            "joint_4_maximum",
            "Joint 4 upper joint limit",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[3]; padded value0 when jointCount < 4",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 4 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(47,
            "joint_4_home",
            "Joint 4 declared home coordinate",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[3]; padded value0 when jointCount < 4",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 4 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(48,
            "joint_4_seed",
            "Joint 4 initial solver coordinate",
            "Raw joint definition",
            "Joint 4 (ordered standard-DH row 4): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[3]; padded value0 when jointCount < 4",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 4th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 4 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 4 joints exist, this slot is exactly0; presence is classification variable#39. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1")
    )
    private fun block4():List<FeatureDefinition> = listOf(
        FeatureDefinition(49,
            "joint_5_present",
            "Joint 5 presence indicator",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 4 < jointCount else0; padded value0 when jointCount < 5",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 5 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(50,
            "joint_5_is_prismatic",
            "Joint 5 prismatic type indicator",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[4]==PRISMATIC else0; padded value0 when jointCount < 5",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 5 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(51,
            "joint_5_dh_theta",
            "Joint 5 static DH theta angle",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[4]; padded value0 when jointCount < 5",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 5 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(52,
            "joint_5_dh_d",
            "Joint 5 static DH d offset",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[4]; padded value0 when jointCount < 5",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 5 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(53,
            "joint_5_dh_a",
            "Joint 5 DH a link distance",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[4]; padded value0 when jointCount < 5",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 5 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(54,
            "joint_5_dh_alpha",
            "Joint 5 DH alpha twist",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[4]; padded value0 when jointCount < 5",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 5 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(55,
            "joint_5_minimum",
            "Joint 5 lower joint limit",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[4]; padded value0 when jointCount < 5",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 5 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(56,
            "joint_5_maximum",
            "Joint 5 upper joint limit",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[4]; padded value0 when jointCount < 5",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 5 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(57,
            "joint_5_home",
            "Joint 5 declared home coordinate",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[4]; padded value0 when jointCount < 5",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 5 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(58,
            "joint_5_seed",
            "Joint 5 initial solver coordinate",
            "Raw joint definition",
            "Joint 5 (ordered standard-DH row 5): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[4]; padded value0 when jointCount < 5",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 5th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 5 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 5 joints exist, this slot is exactly0; presence is classification variable#49. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(59,
            "joint_6_present",
            "Joint 6 presence indicator",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 5 < jointCount else0; padded value0 when jointCount < 6",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 6 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(60,
            "joint_6_is_prismatic",
            "Joint 6 prismatic type indicator",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[5]==PRISMATIC else0; padded value0 when jointCount < 6",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 6 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1")
    )
    private fun block5():List<FeatureDefinition> = listOf(
        FeatureDefinition(61,
            "joint_6_dh_theta",
            "Joint 6 static DH theta angle",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[5]; padded value0 when jointCount < 6",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 6 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(62,
            "joint_6_dh_d",
            "Joint 6 static DH d offset",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[5]; padded value0 when jointCount < 6",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 6 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(63,
            "joint_6_dh_a",
            "Joint 6 DH a link distance",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[5]; padded value0 when jointCount < 6",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 6 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(64,
            "joint_6_dh_alpha",
            "Joint 6 DH alpha twist",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[5]; padded value0 when jointCount < 6",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 6 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(65,
            "joint_6_minimum",
            "Joint 6 lower joint limit",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[5]; padded value0 when jointCount < 6",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 6 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(66,
            "joint_6_maximum",
            "Joint 6 upper joint limit",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[5]; padded value0 when jointCount < 6",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 6 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(67,
            "joint_6_home",
            "Joint 6 declared home coordinate",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[5]; padded value0 when jointCount < 6",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 6 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(68,
            "joint_6_seed",
            "Joint 6 initial solver coordinate",
            "Raw joint definition",
            "Joint 6 (ordered standard-DH row 6): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[5]; padded value0 when jointCount < 6",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 6th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 6 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 6 joints exist, this slot is exactly0; presence is classification variable#59. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(69,
            "joint_7_present",
            "Joint 7 presence indicator",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 6 < jointCount else0; padded value0 when jointCount < 7",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 7 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(70,
            "joint_7_is_prismatic",
            "Joint 7 prismatic type indicator",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[6]==PRISMATIC else0; padded value0 when jointCount < 7",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 7 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(71,
            "joint_7_dh_theta",
            "Joint 7 static DH theta angle",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[6]; padded value0 when jointCount < 7",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 7 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(72,
            "joint_7_dh_d",
            "Joint 7 static DH d offset",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[6]; padded value0 when jointCount < 7",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 7 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1")
    )
    private fun block6():List<FeatureDefinition> = listOf(
        FeatureDefinition(73,
            "joint_7_dh_a",
            "Joint 7 DH a link distance",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[6]; padded value0 when jointCount < 7",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 7 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(74,
            "joint_7_dh_alpha",
            "Joint 7 DH alpha twist",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[6]; padded value0 when jointCount < 7",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 7 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(75,
            "joint_7_minimum",
            "Joint 7 lower joint limit",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[6]; padded value0 when jointCount < 7",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 7 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(76,
            "joint_7_maximum",
            "Joint 7 upper joint limit",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[6]; padded value0 when jointCount < 7",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 7 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(77,
            "joint_7_home",
            "Joint 7 declared home coordinate",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[6]; padded value0 when jointCount < 7",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 7 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(78,
            "joint_7_seed",
            "Joint 7 initial solver coordinate",
            "Raw joint definition",
            "Joint 7 (ordered standard-DH row 7): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[6]; padded value0 when jointCount < 7",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 7th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 7 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 7 joints exist, this slot is exactly0; presence is classification variable#69. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(79,
            "joint_8_present",
            "Joint 8 presence indicator",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 7 < jointCount else0; padded value0 when jointCount < 8",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 8 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(80,
            "joint_8_is_prismatic",
            "Joint 8 prismatic type indicator",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[7]==PRISMATIC else0; padded value0 when jointCount < 8",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 8 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(81,
            "joint_8_dh_theta",
            "Joint 8 static DH theta angle",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[7]; padded value0 when jointCount < 8",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 8 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(82,
            "joint_8_dh_d",
            "Joint 8 static DH d offset",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[7]; padded value0 when jointCount < 8",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 8 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(83,
            "joint_8_dh_a",
            "Joint 8 DH a link distance",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[7]; padded value0 when jointCount < 8",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 8 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(84,
            "joint_8_dh_alpha",
            "Joint 8 DH alpha twist",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[7]; padded value0 when jointCount < 8",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 8 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1")
    )
    private fun block7():List<FeatureDefinition> = listOf(
        FeatureDefinition(85,
            "joint_8_minimum",
            "Joint 8 lower joint limit",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[7]; padded value0 when jointCount < 8",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 8 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(86,
            "joint_8_maximum",
            "Joint 8 upper joint limit",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[7]; padded value0 when jointCount < 8",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 8 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(87,
            "joint_8_home",
            "Joint 8 declared home coordinate",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[7]; padded value0 when jointCount < 8",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 8 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(88,
            "joint_8_seed",
            "Joint 8 initial solver coordinate",
            "Raw joint definition",
            "Joint 8 (ordered standard-DH row 8): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[7]; padded value0 when jointCount < 8",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 8th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 8 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 8 joints exist, this slot is exactly0; presence is classification variable#79. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(89,
            "joint_9_present",
            "Joint 9 presence indicator",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 8 < jointCount else0; padded value0 when jointCount < 9",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 9 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(90,
            "joint_9_is_prismatic",
            "Joint 9 prismatic type indicator",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[8]==PRISMATIC else0; padded value0 when jointCount < 9",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 9 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(91,
            "joint_9_dh_theta",
            "Joint 9 static DH theta angle",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[8]; padded value0 when jointCount < 9",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 9 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(92,
            "joint_9_dh_d",
            "Joint 9 static DH d offset",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[8]; padded value0 when jointCount < 9",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 9 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(93,
            "joint_9_dh_a",
            "Joint 9 DH a link distance",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[8]; padded value0 when jointCount < 9",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 9 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(94,
            "joint_9_dh_alpha",
            "Joint 9 DH alpha twist",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[8]; padded value0 when jointCount < 9",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 9 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(95,
            "joint_9_minimum",
            "Joint 9 lower joint limit",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[8]; padded value0 when jointCount < 9",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 9 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(96,
            "joint_9_maximum",
            "Joint 9 upper joint limit",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[8]; padded value0 when jointCount < 9",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 9 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1")
    )
    private fun block8():List<FeatureDefinition> = listOf(
        FeatureDefinition(97,
            "joint_9_home",
            "Joint 9 declared home coordinate",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[8]; padded value0 when jointCount < 9",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 9 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(98,
            "joint_9_seed",
            "Joint 9 initial solver coordinate",
            "Raw joint definition",
            "Joint 9 (ordered standard-DH row 9): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[8]; padded value0 when jointCount < 9",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 9th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 9 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 9 joints exist, this slot is exactly0; presence is classification variable#89. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(99,
            "joint_10_present",
            "Joint 10 presence indicator",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Distinguishes a real joint from padded input slots.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if 9 < jointCount else0; padded value0 when jointCount < 10",
            "indicator0/1",
            listOf("jointCount"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Distinguishes a real joint from padded input slots.",
            "Candidate input for testing whether joint 10 presence indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero means absent; numeric zeros in other slots alone cannot establish absence. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(100,
            "joint_10_is_prismatic",
            "Joint 10 prismatic type indicator",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Encodes prismatic versus revolute type for a present joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if present and jointTypes[9]==PRISMATIC else0; padded value0 when jointCount < 10",
            "indicator0/1",
            listOf("jointCount","jointTypes"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Encodes prismatic versus revolute type for a present joint.",
            "Candidate input for testing whether joint 10 prismatic type indicator helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Zero may mean revolute or absent; inspect presence. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(101,
            "joint_10_dh_theta",
            "Joint 10 static DH theta angle",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Static theta in the standard-DH row.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhThetaRad[9]; padded value0 when jointCount < 10",
            "rad",
            listOf("dhThetaRad","jointCount"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Static theta in the standard-DH row.",
            "Candidate input for testing whether joint 10 static dh theta angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For REVOLUTE this active DH field must be zero: the seed supplies rotation; for PRISMATIC it is a fixed angle. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(102,
            "joint_10_dh_d",
            "Joint 10 static DH d offset",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Static d translation along the preceding DH z axis.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhDMeters[9]; padded value0 when jointCount < 10",
            "m",
            listOf("dhDMeters","jointCount"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Static d translation along the preceding DH z axis.",
            "Candidate input for testing whether joint 10 static dh d offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For PRISMATIC this active DH field must be zero: the seed supplies translation; for REVOLUTE it is fixed. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(103,
            "joint_10_dh_a",
            "Joint 10 DH a link distance",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAMeters[9]; padded value0 when jointCount < 10",
            "m",
            listOf("dhAMeters","jointCount"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Signed standard-DH link a translation; retained exactly, not an absolute length.",
            "Candidate input for testing whether joint 10 dh a link distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "DH parameter alone is not overall reach and depends on the declared frame convention. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(104,
            "joint_10_dh_alpha",
            "Joint 10 DH alpha twist",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Static standard-DH twist angle between successive z axes.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "dhAlphaRad[9]; padded value0 when jointCount < 10",
            "rad",
            listOf("dhAlphaRad","jointCount"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Static standard-DH twist angle between successive z axes.",
            "Candidate input for testing whether joint 10 dh alpha twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Periodic angle is kept raw in this baseline slot. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(105,
            "joint_10_minimum",
            "Joint 10 lower joint limit",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Lower allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMinValues[9]; padded value0 when jointCount < 10",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMinValues","jointTypes","jointCount"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Lower allowed coordinate of this joint.",
            "Candidate input for testing whether joint 10 lower joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must be strictly below maximum; unit depends on joint type. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(106,
            "joint_10_maximum",
            "Joint 10 upper joint limit",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Upper allowed coordinate of this joint.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointMaxValues[9]; padded value0 when jointCount < 10",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointMaxValues","jointTypes","jointCount"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Upper allowed coordinate of this joint.",
            "Candidate input for testing whether joint 10 upper joint limit helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Must exceed minimum; not an unconstrained travel capability. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(107,
            "joint_10_home",
            "Joint 10 declared home coordinate",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Saved reference/home coordinate, constrained inside limits.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "jointHomeValues[9]; padded value0 when jointCount < 10",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("jointHomeValues","jointTypes","jointCount"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Saved reference/home coordinate, constrained inside limits.",
            "Candidate input for testing whether joint 10 declared home coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Home is not necessarily the initial seed or a physical calibrated zero. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1"),
        FeatureDefinition(108,
            "joint_10_seed",
            "Joint 10 initial solver coordinate",
            "Raw joint definition",
            "Joint 10 (ordered standard-DH row 10): Initial coordinate supplied to IK before any iteration.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedJointValues[9]; padded value0 when jointCount < 10",
            "rad for REVOLUTE; m for PRISMATIC",
            listOf("seedJointValues","jointTypes","jointCount"),
            "Applies specifically to the 10th serial joint, not a robot or CSV row index. Initial coordinate supplied to IK before any iteration.",
            "Candidate input for testing whether joint 10 initial solver coordinate helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a joint-state seed, not the random-number generator seed or solved coordinate. If fewer than 10 joints exist, this slot is exactly0; presence is classification variable#99. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:317–329 · ordered joint loop",
            "feature-dictionary-v1")
    )
    private fun block9():List<FeatureDefinition> = listOf(
        FeatureDefinition(109,
            "initial_cartesian_error",
            "Initial Cartesian distance",
            "Stored seed context",
            "Distance between seed FK end point and requested target, imported from pre-solve metric.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "initialError if finite else0",
            "m",
            listOf("initialError"),
            "Distance between seed FK end point and requested target, imported from pre-solve metric.",
            "Candidate input for testing whether initial cartesian distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Generated by DiagnosticRunMetricsCalculator.calculateInitialError: distance(FK(seed),target) when FK valid, otherwise NaN. Missing/nonfinite becomes0; pair with initial_error_available. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block; app/src/main/java/com/robotkinematicslab/mobile/diagnostics/metrics/DiagnosticRunMetricsCalculator.kt:40",
            "feature-dictionary-v1"),
        FeatureDefinition(110,
            "initial_error_available",
            "Initial-error availability",
            "Stored seed context",
            "Distinguishes observed initial distance from zero imputation.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if initialError finite else0",
            "indicator0/1",
            listOf("initialError"),
            "Distinguishes observed initial distance from zero imputation.",
            "Candidate input for testing whether initial-error availability helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 only means a finite CSV value exists, not that target is reachable. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(111,
            "seed_min_normalized_limit_margin",
            "Smallest seed joint-limit margin",
            "Stored seed context",
            "Stored minimum over joints of distance to the nearest limit divided by full joint span.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "seedMinNormalizedLimitMargin if finite else0",
            "dimensionless",
            listOf("seedMinNormalizedLimitMargin"),
            "Stored minimum over joints of distance to the nearest limit divided by full joint span.",
            "Candidate input for testing whether smallest seed joint-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Generator computes min_j clamp(min((q-min)/span,(max-q)/span),0,0.5); invalid range/state yieldsNaN. Missing becomes0; inspect seed_margin_available. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block; app/src/main/java/com/robotkinematicslab/mobile/diagnostics/metrics/DiagnosticDerivedMetricsCalculator.kt:58,155",
            "feature-dictionary-v1"),
        FeatureDefinition(112,
            "seed_margin_available",
            "Seed-margin availability",
            "Stored seed context",
            "Finite-value availability of the stored seed-limit metric.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "1 if seedMinNormalizedLimitMargin finite else0",
            "indicator0/1",
            listOf("seedMinNormalizedLimitMargin"),
            "Finite-value availability of the stored seed-limit metric.",
            "Candidate input for testing whether seed-margin availability helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Does not distinguish exact boundary0 from missing0 without the paired value. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(113,
            "seed_log_condition_number",
            "Stored base-ten log seed condition",
            "Stored seed context",
            "Stored pre-solve condition compressed by decimal logarithm.",
            "Imported scientific CSV seedLogConditionNumber, derived from solverDiagnostics.seedConditionNumber. This recorded diagnostic is populated only if solver execution reaches Jacobian/condition analysis; early convergence can leave it NaN.",
            "seedLogConditionNumber if finite else0",
            "base10 log of dimensionless condition",
            listOf("seedLogConditionNumber"),
            "Stored pre-solve condition compressed by decimal logarithm.",
            "Audit diagnostic availability and predictor admissibility for the intended task. Any classification benefit must be checked for execution-path leakage; compare with a fresh pre-solve condition computation rather than assuming the recorded field is available at prediction time.",
            "Generator uses log10(condition) clamped[0,policy cap], defaultcap12; +Infinity→cap, NaN/nonpositive→missing. This differs from natural-log recomputed expanded condition. Imported finite values are accepted as encoded. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula. ADMISSIBILITY WARNING: availability depends on solver execution path, not solely on a guaranteed independent pre-solve computation. Early success before Jacobian analysis can yield availability0; therefore this flag can correlate with the outcome through control flow. Do not treat the stored value/flag as automatically safe prospective predictors. Compare with independently recomputed expanded Jacobian features under the intended prediction protocol.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block; app/src/main/java/com/robotkinematicslab/mobile/diagnostics/metrics/DiagnosticDerivedMetricsCalculator.kt:58,155; app/src/main/java/com/robotkinematicslab/mobile/solver/ik/InverseKinematicsSolver.kt:229,293,386",
            "feature-dictionary-v1"),
        FeatureDefinition(114,
            "seed_condition_available",
            "Seed-condition availability",
            "Stored seed context",
            "Marks whether the stored seed-condition logarithm was finite.",
            "Imported scientific CSV seedLogConditionNumber, derived from solverDiagnostics.seedConditionNumber. This recorded diagnostic is populated only if solver execution reaches Jacobian/condition analysis; early convergence can leave it NaN.",
            "1 if seedLogConditionNumber finite else0",
            "indicator0/1",
            listOf("seedLogConditionNumber"),
            "Marks whether the stored seed-condition logarithm was finite.",
            "Audit diagnostic availability and predictor admissibility for the intended task. Any classification benefit must be checked for execution-path leakage; compare with a fresh pre-solve condition computation rather than assuming the recorded field is available at prediction time.",
            "1 does not exclude capped infinite condition; the generator maps +Infinity to cap12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula. ADMISSIBILITY WARNING: availability depends on solver execution path, not solely on a guaranteed independent pre-solve computation. Early success before Jacobian analysis can yield availability0; therefore this flag can correlate with the outcome through control flow. Do not treat the stored value/flag as automatically safe prospective predictors. Compare with independently recomputed expanded Jacobian features under the intended prediction protocol.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block; app/src/main/java/com/robotkinematicslab/mobile/solver/ik/InverseKinematicsSolver.kt:229,293,386",
            "feature-dictionary-v1"),
        FeatureDefinition(115,
            "target_radius",
            "Target distance from base origin",
            "Initial geometric context",
            "Euclidean norm of requested position in the base frame.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "hypot(hypot(targetX,targetY),targetZ)",
            "m",
            listOf("targetX","targetY","targetZ"),
            "Euclidean norm of requested position in the base frame.",
            "Candidate input for testing whether target distance from base origin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "A radial scalar loses directional and orientation information. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(116,
            "conservative_reach_bound",
            "Conservative serial-chain extent sum",
            "Initial geometric context",
            "Geometry-based outer extent bound using maximal prismatic travel magnitude.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "sum hypot(dhAMeters[j],if PRISMATIC max(abs(jointMinValues[j]),abs(jointMaxValues[j])) else abs(dhDMeters[j])); floor1e-12",
            "m",
            listOf("dhAMeters","dhDMeters","jointTypes","jointMinValues","jointMaxValues"),
            "Geometry-based outer extent bound using maximal prismatic travel magnitude.",
            "Candidate input for testing whether conservative serial-chain extent sum helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "An outer bound is not proof of workspace membership; angular and coupled constraints remain. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(117,
            "target_reach_ratio",
            "Target radius divided by extent bound",
            "Initial geometric context",
            "Radial target utilization of the conservative bound.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "target_radius / conservative_reach_bound",
            "dimensionless",
            listOf("targetX","targetY","targetZ","dhAMeters","dhDMeters","jointTypes","jointMinValues","jointMaxValues"),
            "Radial target utilization of the conservative bound.",
            "Candidate input for testing whether target radius divided by extent bound helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Ratios below1 do not certify reachability. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(118,
            "prismatic_joint_fraction",
            "Prismatic fraction",
            "Initial geometric context",
            "Fraction of present joints with translational coordinates.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "count(PRISMATIC)/jointCount",
            "dimensionless",
            listOf("jointTypes","jointCount"),
            "Fraction of present joints with translational coordinates.",
            "Candidate input for testing whether prismatic fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Complement of revolute_joint_fraction for admitted types, not an independent DOF count. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(119,
            "revolute_joint_fraction",
            "Revolute fraction",
            "Initial geometric context",
            "Fraction of present joints with rotational coordinates.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "(jointCount-count(PRISMATIC))/jointCount",
            "dimensionless",
            listOf("jointTypes","jointCount"),
            "Fraction of present joints with rotational coordinates.",
            "Candidate input for testing whether revolute fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Exact complement of prismatic_joint_fraction in this valid schema. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(120,
            "mean_link_extent",
            "Mean static DH extent",
            "Initial geometric context",
            "Average static per-row DH translation magnitude.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "mean(hypot(dhAMeters[j],dhDMeters[j]))",
            "m",
            listOf("dhAMeters","dhDMeters"),
            "Average static per-row DH translation magnitude.",
            "Candidate input for testing whether mean static dh extent helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic active d is zero in stored DH, so this excludes current extension. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1")
    )
    private fun block10():List<FeatureDefinition> = listOf(
        FeatureDefinition(121,
            "link_extent_standard_deviation",
            "Dispersion of static DH extents",
            "Initial geometric context",
            "Spread of static translation extents across the present chain.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "population std(hypot(dhAMeters[j],dhDMeters[j]))",
            "m",
            listOf("dhAMeters","dhDMeters"),
            "Spread of static translation extents across the present chain.",
            "Candidate input for testing whether dispersion of static dh extents helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Population denominatorN; zero with one joint. Prismatic extension is excluded. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(122,
            "mean_joint_span",
            "Mean raw joint coordinate span",
            "Initial geometric context",
            "Mean of each raw maximum-minus-minimum span.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "mean(jointMaxValues-jointMinValues)",
            "rad/m mixed when topology mixed",
            listOf("jointMinValues","jointMaxValues"),
            "Mean of each raw maximum-minus-minimum span.",
            "Candidate input for testing whether mean raw joint coordinate span helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "A mixed topology combines radians and metres; this numeric statistic has no single physical unit. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(123,
            "minimum_joint_span",
            "Smallest raw joint coordinate span",
            "Initial geometric context",
            "Smallest declared coordinate span in numeric internal units.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "min(jointMaxValues-jointMinValues)",
            "rad/m mixed when topology mixed",
            listOf("jointMinValues","jointMaxValues"),
            "Smallest declared coordinate span in numeric internal units.",
            "Candidate input for testing whether smallest raw joint coordinate span helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Comparing radians to metres in mixed chains is a model feature, not a physical dimensionless comparison. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(124,
            "normalized_target_x",
            "Reach-scaled target x",
            "Initial geometric context",
            "Target x expressed relative to conservative chain extent.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "targetX / conservative_reach_bound",
            "dimensionless",
            listOf("targetX","dhAMeters","dhDMeters","jointTypes","jointMinValues","jointMaxValues"),
            "Target x expressed relative to conservative chain extent.",
            "Candidate input for testing whether reach-scaled target x helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Bound denominatorfloored1e-12, not true directional workspace size. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(125,
            "normalized_target_y",
            "Reach-scaled target y",
            "Initial geometric context",
            "Target y expressed relative to conservative chain extent.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "targetY / conservative_reach_bound",
            "dimensionless",
            listOf("targetY","dhAMeters","dhDMeters","jointTypes","jointMinValues","jointMaxValues"),
            "Target y expressed relative to conservative chain extent.",
            "Candidate input for testing whether reach-scaled target y helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Bound denominatorfloored1e-12, not true directional workspace size. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(126,
            "normalized_target_z",
            "Reach-scaled target z",
            "Initial geometric context",
            "Target z expressed relative to conservative chain extent.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "targetZ / conservative_reach_bound",
            "dimensionless",
            listOf("targetZ","dhAMeters","dhDMeters","jointTypes","jointMinValues","jointMaxValues"),
            "Target z expressed relative to conservative chain extent.",
            "Candidate input for testing whether reach-scaled target z helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Bound denominatorfloored1e-12, not true directional workspace size. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(127,
            "seed_home_offset_rms",
            "RMS seed-to-home full-span offset",
            "Initial geometric context",
            "Root-mean-square seed displacement from home normalized by full spans.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "sqrt(mean(((seedJointValues-jointHomeValues)/(jointMaxValues-jointMinValues))^2))",
            "dimensionless",
            listOf("seedJointValues","jointHomeValues","jointMinValues","jointMaxValues"),
            "Root-mean-square seed displacement from home normalized by full spans.",
            "Candidate input for testing whether rms seed-to-home full-span offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Full-span denominator; expanded per-joint seed_home_delta_normalized uses half spans and is twice as large. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(128,
            "seed_home_offset_mean_absolute",
            "Mean absolute seed-to-home full-span offset",
            "Initial geometric context",
            "Average unsigned displacement from home as a fraction of each complete allowed span.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "mean(abs((seedJointValues-jointHomeValues)/(jointMaxValues-jointMinValues)))",
            "dimensionless",
            listOf("seedJointValues","jointHomeValues","jointMinValues","jointMaxValues"),
            "Average unsigned displacement from home as a fraction of each complete allowed span.",
            "Candidate input for testing whether mean absolute seed-to-home full-span offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Signs are discarded; does not measure travel during IK. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(129,
            "seed_home_offset_max_absolute",
            "Maximum seed-to-home full-span offset",
            "Initial geometric context",
            "Largest normalized initial displacement among present joints.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "max(abs((seedJointValues-jointHomeValues)/(jointMaxValues-jointMinValues)))",
            "dimensionless",
            listOf("seedJointValues","jointHomeValues","jointMinValues","jointMaxValues"),
            "Largest normalized initial displacement among present joints.",
            "Candidate input for testing whether maximum seed-to-home full-span offset helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "A maximum summarizes one joint and hides which joint produced it. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(130,
            "workspace_boundary_proximity",
            "Distance from unit radial reach ratio",
            "Initial geometric context",
            "Absolute separation from the conservative outer radial boundary in normalized coordinates.",
            "Encoded before IK from scientific CSV inputs. Read or transformed from the named CSV columns.",
            "abs(1-target_radius/conservative_reach_bound)",
            "dimensionless",
            listOf("targetX","targetY","targetZ","dhAMeters","dhDMeters","jointTypes","jointMinValues","jointMaxValues"),
            "Absolute separation from the conservative outer radial boundary in normalized coordinates.",
            "Candidate input for testing whether distance from unit radial reach ratio helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Despite its name, this is a distance (smaller means nearer); ignores inner voids and full workspace geometry. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ScientificDatasetTrainingReader.kt:332–387 · context block",
            "feature-dictionary-v1"),
        FeatureDefinition(131,
            "seed_end_effector_x",
            "Seed end effector x",
            "Seed task geometry",
            "Seed end-effector x coordinate from fresh standard-DH FK.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "kinematics.endEffector[0]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointTypes","seedJointValues"),
            "Seed end-effector x coordinate from fresh standard-DH FK.",
            "Candidate input for testing whether seed end effector x helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(132,
            "seed_end_effector_y",
            "Seed end effector y",
            "Seed task geometry",
            "Seed end-effector y coordinate from fresh standard-DH FK.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "kinematics.endEffector[1]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointTypes","seedJointValues"),
            "Seed end-effector y coordinate from fresh standard-DH FK.",
            "Candidate input for testing whether seed end effector y helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[1]",
            "feature-dictionary-v1")
    )
    private fun block11():List<FeatureDefinition> = listOf(
        FeatureDefinition(133,
            "seed_end_effector_z",
            "Seed end effector z",
            "Seed task geometry",
            "Seed end-effector z coordinate from fresh standard-DH FK.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "kinematics.endEffector[2]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointTypes","seedJointValues"),
            "Seed end-effector z coordinate from fresh standard-DH FK.",
            "Candidate input for testing whether seed end effector z helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(134,
            "normalized_seed_end_effector_x",
            "Normalized seed end effector x",
            "Seed task geometry",
            "Seed FK x divided by conservative chain extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "kinematics.endEffector[0] / reach; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Seed FK x divided by conservative chain extent.",
            "Candidate input for testing whether normalized seed end effector x helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(135,
            "normalized_seed_end_effector_y",
            "Normalized seed end effector y",
            "Seed task geometry",
            "Seed FK y divided by conservative chain extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "kinematics.endEffector[1] / reach; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Seed FK y divided by conservative chain extent.",
            "Candidate input for testing whether normalized seed end effector y helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(136,
            "normalized_seed_end_effector_z",
            "Normalized seed end effector z",
            "Seed task geometry",
            "Seed FK z divided by conservative chain extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "kinematics.endEffector[2] / reach; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Seed FK z divided by conservative chain extent.",
            "Candidate input for testing whether normalized seed end effector z helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(137,
            "seed_target_error_x",
            "Seed target error x",
            "Seed task geometry",
            "Signed x error from seed end point toward target.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "error[0]; error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Signed x error from seed end point toward target.",
            "Candidate input for testing whether seed target error x helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(138,
            "seed_target_error_y",
            "Seed target error y",
            "Seed task geometry",
            "Signed y error from seed end point toward target.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "error[1]; error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Signed y error from seed end point toward target.",
            "Candidate input for testing whether seed target error y helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(139,
            "seed_target_error_z",
            "Seed target error z",
            "Seed task geometry",
            "Signed z error from seed end point toward target.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "error[2]; error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Signed z error from seed end point toward target.",
            "Candidate input for testing whether seed target error z helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(140,
            "normalized_seed_target_error_x",
            "Normalized seed target error x",
            "Seed task geometry",
            "Signed x seed error scaled by conservative reach.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "error[0] / reach; error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Signed x seed error scaled by conservative reach.",
            "Candidate input for testing whether normalized seed target error x helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(141,
            "normalized_seed_target_error_y",
            "Normalized seed target error y",
            "Seed task geometry",
            "Signed y seed error scaled by conservative reach.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "error[1] / reach; error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Signed y seed error scaled by conservative reach.",
            "Candidate input for testing whether normalized seed target error y helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(142,
            "normalized_seed_target_error_z",
            "Normalized seed target error z",
            "Seed task geometry",
            "Signed z seed error scaled by conservative reach.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "error[2] / reach; error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Signed z seed error scaled by conservative reach.",
            "Candidate input for testing whether normalized seed target error z helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(143,
            "recomputed_initial_error",
            "Recomputed initial error",
            "Seed task geometry",
            "Euclidean seed-to-target error recomputed from geometry, independently of the stored initialError column.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "errorNorm; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Euclidean seed-to-target error recomputed from geometry, independently of the stored initialError column.",
            "Candidate input for testing whether recomputed initial error helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[12]",
            "feature-dictionary-v1"),
        FeatureDefinition(144,
            "normalized_initial_error",
            "Normalized initial error",
            "Seed task geometry",
            "Fresh seed error divided by conservative chain extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "errorNorm / reach; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Fresh seed error divided by conservative chain extent.",
            "Candidate input for testing whether normalized initial error helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[13]",
            "feature-dictionary-v1")
    )
    private fun block12():List<FeatureDefinition> = listOf(
        FeatureDefinition(145,
            "target_cylindrical_radius",
            "Target cylindrical radius",
            "Seed task geometry",
            "Target cylindrical distance from the base z axis.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "targetCylindricalRadius; targetCylindricalRadius = hypot(targetX,targetY)",
            "m",
            listOf("targetX","targetY"),
            "Target cylindrical distance from the base z axis.",
            "Candidate input for testing whether target cylindrical radius helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(146,
            "normalized_target_cylindrical_radius",
            "Normalized target cylindrical radius",
            "Seed task geometry",
            "Cylindrical target radius divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "targetCylindricalRadius / reach; targetCylindricalRadius = hypot(targetX,targetY); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","targetX","targetY"),
            "Cylindrical target radius divided by conservative extent.",
            "Candidate input for testing whether normalized target cylindrical radius helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[15]",
            "feature-dictionary-v1"),
        FeatureDefinition(147,
            "target_azimuth_sin",
            "Target azimuth sin",
            "Seed task geometry",
            "Sine of target azimuth atan2(y,x).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "safeDirectionComponent(input.targetY, input.targetX, true); safeDirectionComponent = atan2(targetY,targetX), then sine/cosine;0 when hypot(targetX,targetY)<=EPS; EPS = 1e-12",
            "dimensionless",
            listOf("targetX","targetY"),
            "Sine of target azimuth atan2(y,x).",
            "Candidate input for testing whether target azimuth sin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[16]",
            "feature-dictionary-v1"),
        FeatureDefinition(148,
            "target_azimuth_cos",
            "Target azimuth cos",
            "Seed task geometry",
            "Cosine of target azimuth atan2(y,x).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "safeDirectionComponent(input.targetY, input.targetX, false); safeDirectionComponent = atan2(targetY,targetX), then sine/cosine;0 when hypot(targetX,targetY)<=EPS; EPS = 1e-12",
            "dimensionless",
            listOf("targetX","targetY"),
            "Cosine of target azimuth atan2(y,x).",
            "Candidate input for testing whether target azimuth cos helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[17]",
            "feature-dictionary-v1"),
        FeatureDefinition(149,
            "target_elevation_sin",
            "Target elevation sin",
            "Seed task geometry",
            "Sine of target elevation atan2(z,cylindrical radius).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "safeElevationComponent(targetCylindricalRadius, input.targetZ, true); safeElevationComponent = atan2(targetZ,targetCylindricalRadius), then sine/cosine;0 when hypot(radius,targetZ)<=EPS; targetCylindricalRadius = hypot(targetX,targetY); EPS = 1e-12",
            "dimensionless",
            listOf("targetX","targetY","targetZ"),
            "Sine of target elevation atan2(z,cylindrical radius).",
            "Candidate input for testing whether target elevation sin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[18]",
            "feature-dictionary-v1"),
        FeatureDefinition(150,
            "target_elevation_cos",
            "Target elevation cos",
            "Seed task geometry",
            "Cosine of target elevation atan2(z,cylindrical radius).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "safeElevationComponent(targetCylindricalRadius, input.targetZ, false); safeElevationComponent = atan2(targetZ,targetCylindricalRadius), then sine/cosine;0 when hypot(radius,targetZ)<=EPS; targetCylindricalRadius = hypot(targetX,targetY); EPS = 1e-12",
            "dimensionless",
            listOf("targetX","targetY","targetZ"),
            "Cosine of target elevation atan2(z,cylindrical radius).",
            "Candidate input for testing whether target elevation cos helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[19]",
            "feature-dictionary-v1"),
        FeatureDefinition(151,
            "normalized_seed_radius",
            "Normalized seed radius",
            "Seed task geometry",
            "Seed end-point distance from origin divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "norm3(kinematics.endEffector[0], kinematics.endEffector[1], kinematics.endEffector[2]) / reach; norm3 = hypot(hypot(x,y),z); kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Seed end-point distance from origin divided by conservative extent.",
            "Candidate input for testing whether normalized seed radius helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[20]",
            "feature-dictionary-v1"),
        FeatureDefinition(152,
            "normalized_target_seed_radial_delta",
            "Normalized target seed radial delta",
            "Seed task geometry",
            "Signed difference between target and seed radial distances divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "(targetRadius - norm3(kinematics.endEffector[0], kinematics.endEffector[1], kinematics.endEffector[2])) / reach; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z); kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Signed difference between target and seed radial distances divided by conservative extent.",
            "Candidate input for testing whether normalized target seed radial delta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "All quantities refer to the initial seed, not final IK state. Angular sine/cosine returns0 when direction is undefined at radius<=1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[21]",
            "feature-dictionary-v1"),
        FeatureDefinition(153,
            "jacobian_sigma_max",
            "Jacobian sigma max",
            "Scaled Jacobian spectrum",
            "Largest singular value of the half-span-scaled positional Jacobian.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "sigmaMax; sigmaMax = singularValues[0]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Largest singular value of the half-span-scaled positional Jacobian.",
            "Candidate input for testing whether jacobian sigma max helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[22]",
            "feature-dictionary-v1"),
        FeatureDefinition(154,
            "jacobian_sigma_middle",
            "Jacobian sigma middle",
            "Scaled Jacobian spectrum",
            "Middle of the three task-space singular values.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "sigmaMiddle; sigmaMiddle = singularValues[1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Middle of the three task-space singular values.",
            "Candidate input for testing whether jacobian sigma middle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[23]",
            "feature-dictionary-v1"),
        FeatureDefinition(155,
            "jacobian_sigma_third",
            "Jacobian sigma third",
            "Scaled Jacobian spectrum",
            "Third/smallest task-space singular value, including structural zero for fewer than3 DOFs.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "sigmaThird; sigmaThird = singularValues[2]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Third/smallest task-space singular value, including structural zero for fewer than3 DOFs.",
            "Candidate input for testing whether jacobian sigma third helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[24]",
            "feature-dictionary-v1"),
        FeatureDefinition(156,
            "log_jacobian_sigma_max",
            "Log jacobian sigma max",
            "Scaled Jacobian spectrum",
            "Natural logarithm of the largest scaled Jacobian singular value.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "safeLog(sigmaMax); safeLog = ln(clamp(value,EPS,CONDITION_CAP)); natural logarithm of implementation numeric value; EPS = 1e-12; CONDITION_CAP = 1e12; sigmaMax = singularValues[0]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "ln of numeric metres",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Natural logarithm of the largest scaled Jacobian singular value.",
            "Candidate input for testing whether log jacobian sigma max helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[25]",
            "feature-dictionary-v1")
    )
    private fun block13():List<FeatureDefinition> = listOf(
        FeatureDefinition(157,
            "log_jacobian_sigma_middle",
            "Log jacobian sigma middle",
            "Scaled Jacobian spectrum",
            "Natural logarithm of the middle scaled singular value.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "safeLog(sigmaMiddle); safeLog = ln(clamp(value,EPS,CONDITION_CAP)); natural logarithm of implementation numeric value; EPS = 1e-12; CONDITION_CAP = 1e12; sigmaMiddle = singularValues[1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "ln of numeric metres",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Natural logarithm of the middle scaled singular value.",
            "Candidate input for testing whether log jacobian sigma middle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[26]",
            "feature-dictionary-v1"),
        FeatureDefinition(158,
            "log_jacobian_sigma_third",
            "Log jacobian sigma third",
            "Scaled Jacobian spectrum",
            "Natural logarithm of the third scaled singular value.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "safeLog(sigmaThird); safeLog = ln(clamp(value,EPS,CONDITION_CAP)); natural logarithm of implementation numeric value; EPS = 1e-12; CONDITION_CAP = 1e12; sigmaThird = singularValues[2]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "ln of numeric metres",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Natural logarithm of the third scaled singular value.",
            "Candidate input for testing whether log jacobian sigma third helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[27]",
            "feature-dictionary-v1"),
        FeatureDefinition(159,
            "jacobian_condition_number",
            "Jacobian condition number",
            "Scaled Jacobian spectrum",
            "Capped ratio of largest to minimum expected-rank singular value.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "condition.coerceAtMost(CONDITION_CAP); condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; sigmaExpectedMin = singularValues[expectedRank-1]; expectedRank = clamp(count,1,3); count = jointTypes.size; CONDITION_CAP = 1e12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Capped ratio of largest to minimum expected-rank singular value.",
            "Candidate input for testing whether jacobian condition number helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[28]",
            "feature-dictionary-v1"),
        FeatureDefinition(160,
            "log_recomputed_jacobian_condition",
            "Log recomputed jacobian condition",
            "Scaled Jacobian spectrum",
            "Natural logarithm of recomputed condition, unlike the stored base-ten context logarithm.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "safeLog(condition); safeLog = ln(clamp(value,EPS,CONDITION_CAP)); natural logarithm of implementation numeric value; EPS = 1e-12; CONDITION_CAP = 1e12; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; sigmaExpectedMin = singularValues[expectedRank-1]; expectedRank = clamp(count,1,3); count = jointTypes.size",
            "ln of dimensionless condition",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Natural logarithm of recomputed condition, unlike the stored base-ten context logarithm.",
            "Candidate input for testing whether log recomputed jacobian condition helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[29]",
            "feature-dictionary-v1"),
        FeatureDefinition(161,
            "jacobian_reciprocal_condition",
            "Jacobian reciprocal condition",
            "Scaled Jacobian spectrum",
            "Reciprocal of the recomputed expected-rank condition.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "reciprocalCondition; reciprocalCondition = 1 / condition if condition > EPS, else0; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; sigmaExpectedMin = singularValues[expectedRank-1]; expectedRank = clamp(count,1,3); count = jointTypes.size; CONDITION_CAP = 1e12",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Reciprocal of the recomputed expected-rank condition.",
            "Candidate input for testing whether jacobian reciprocal condition helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[30]",
            "feature-dictionary-v1"),
        FeatureDefinition(162,
            "jacobian_manipulability",
            "Jacobian manipulability",
            "Scaled Jacobian spectrum",
            "Product of the first min(jointCount,3) scaled singular values.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "manipulability; manipulability = product(first expectedRank singularValues); expectedRank = clamp(count,1,3); count = jointTypes.size; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "m^min(jointCount,3)",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Product of the first min(jointCount,3) scaled singular values.",
            "Candidate input for testing whether jacobian manipulability helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[31]",
            "feature-dictionary-v1"),
        FeatureDefinition(163,
            "log_jacobian_manipulability",
            "Log jacobian manipulability",
            "Scaled Jacobian spectrum",
            "Natural log of that expected-rank singular-value product.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "safeLog(manipulability); safeLog = ln(clamp(value,EPS,CONDITION_CAP)); natural logarithm of implementation numeric value; EPS = 1e-12; CONDITION_CAP = 1e12; manipulability = product(first expectedRank singularValues); expectedRank = clamp(count,1,3); count = jointTypes.size; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "ln of numeric manipulability",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Natural log of that expected-rank singular-value product.",
            "Candidate input for testing whether log jacobian manipulability helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[32]",
            "feature-dictionary-v1"),
        FeatureDefinition(164,
            "jacobian_spectral_energy",
            "Jacobian spectral energy",
            "Scaled Jacobian spectrum",
            "Sum of squared scaled singular values; total positional Jacobian energy.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "spectralEnergy; spectralEnergy = sum(singularValues^2); singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "m²",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Sum of squared scaled singular values; total positional Jacobian energy.",
            "Candidate input for testing whether jacobian spectral energy helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[33]",
            "feature-dictionary-v1"),
        FeatureDefinition(165,
            "jacobian_spectral_entropy",
            "Jacobian spectral entropy",
            "Scaled Jacobian spectrum",
            "Shannon entropy of normalized squared singular values.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "spectralEntropy; spectralEntropy = -sum(w*ln(w) for w in spectralWeights when w>EPS); spectralWeights = singularValues^2 / spectralEnergy if spectralEnergy > EPS, else three zeros; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; spectralEnergy = sum(singularValues^2)",
            "nats",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Shannon entropy of normalized squared singular values.",
            "Candidate input for testing whether jacobian spectral entropy helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[34]",
            "feature-dictionary-v1"),
        FeatureDefinition(166,
            "jacobian_effective_rank",
            "Jacobian effective rank",
            "Scaled Jacobian spectrum",
            "Exponential spectral entropy as an effective-rank statistic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "effectiveRank; effectiveRank = exp(spectralEntropy); spectralEntropy = -sum(w*ln(w) for w in spectralWeights when w>EPS); spectralWeights = singularValues^2 / spectralEnergy if spectralEnergy > EPS, else three zeros; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; spectralEnergy = sum(singularValues^2)",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Exponential spectral entropy as an effective-rank statistic.",
            "Candidate input for testing whether jacobian effective rank helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[35]",
            "feature-dictionary-v1"),
        FeatureDefinition(167,
            "jacobian_task_rank_ratio",
            "Jacobian task rank ratio",
            "Scaled Jacobian spectrum",
            "Number of singular values above the relative/absolute threshold divided by3.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "observedRank / 3.0; observedRank = count(singularValues > rankThreshold); count = jointTypes.size; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; rankThreshold = max(EPS,sigmaMax*1e-9); sigmaMax = singularValues[0]",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Number of singular values above the relative/absolute threshold divided by3.",
            "Candidate input for testing whether jacobian task rank ratio helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[36]",
            "feature-dictionary-v1"),
        FeatureDefinition(168,
            "jacobian_isotropy",
            "Jacobian isotropy",
            "Scaled Jacobian spectrum",
            "Minimum expected-rank singular value divided by maximum.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "if (sigmaMax > EPS) sigmaExpectedMin / sigmaMax else 0.0; sigmaMax = singularValues[0]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; sigmaExpectedMin = singularValues[expectedRank-1]; expectedRank = clamp(count,1,3); count = jointTypes.size",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Minimum expected-rank singular value divided by maximum.",
            "Candidate input for testing whether jacobian isotropy helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[37]",
            "feature-dictionary-v1")
    )
    private fun block14():List<FeatureDefinition> = listOf(
        FeatureDefinition(169,
            "jacobian_middle_to_max_ratio",
            "Jacobian middle to max ratio",
            "Scaled Jacobian spectrum",
            "Middle singular value divided by maximum.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "if (sigmaMax > EPS) sigmaMiddle / sigmaMax else 0.0; sigmaMax = singularValues[0]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; sigmaMiddle = singularValues[1]",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Middle singular value divided by maximum.",
            "Candidate input for testing whether jacobian middle to max ratio helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Positional3xN Jacobian is scaled by joint half spans. Expected rank=min(N,3), observed rank uses max(1e-12,sigmaMax*1e-9). Zero Gram gives observed rank0 but entropy0 and effective rank1; this statistic is not algebraic rank. safeLog clamps[1e-12,1e12]; condition fallback1e12 is a convention, not a measured finite inverse. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[38]",
            "feature-dictionary-v1"),
        FeatureDefinition(170,
            "directional_manipulability",
            "Directional manipulability",
            "Directional DLS preview",
            "Square root of the error-direction quadratic form of scaled Jacobian Gram.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "directionalMobility; directionalMobility = sqrt(max(0,directionalMobilitySquared)); directionalMobilitySquared = transpose(errorDirection)*gram*errorDirection; errorDirection = error / errorNorm if errorNorm > EPS; otherwise (0,0,0); error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; errorNorm = hypot(hypot(error[0],error[1]),error[2]); EPS = 1e-12; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Square root of the error-direction quadratic form of scaled Jacobian Gram.",
            "Candidate input for testing whether directional manipulability helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[39]",
            "feature-dictionary-v1"),
        FeatureDefinition(171,
            "normalized_directional_manipulability",
            "Normalized directional manipulability",
            "Directional DLS preview",
            "Directional mobility divided by the maximum scaled singular value.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "directionalMobility / max(sigmaMax, EPS); directionalMobility = sqrt(max(0,directionalMobilitySquared)); directionalMobilitySquared = transpose(errorDirection)*gram*errorDirection; errorDirection = error / errorNorm if errorNorm > EPS; otherwise (0,0,0); error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; errorNorm = hypot(hypot(error[0],error[1]),error[2]); EPS = 1e-12; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; sigmaMax = singularValues[0]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Directional mobility divided by the maximum scaled singular value.",
            "Candidate input for testing whether normalized directional manipulability helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[40]",
            "feature-dictionary-v1"),
        FeatureDefinition(172,
            "dls_raw_step_l2",
            "DLS raw step l2",
            "Directional DLS preview",
            "Euclidean norm of un-clipped preview DLS raw joint-coordinate increments.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "dls.rawJointStepNorm; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "rad/m mixed for mixed topology",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Euclidean norm of un-clipped preview DLS raw joint-coordinate increments.",
            "Candidate input for testing whether dls raw step l2 helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[41]",
            "feature-dictionary-v1"),
        FeatureDefinition(173,
            "dls_raw_step_rms",
            "DLS raw step rms",
            "Directional DLS preview",
            "RMS of the raw preview joint increments.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "dls.rawJointStepNorm / sqrt(count.toDouble()); dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "rad/m mixed for mixed topology",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "RMS of the raw preview joint increments.",
            "Candidate input for testing whether dls raw step rms helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[42]",
            "feature-dictionary-v1"),
        FeatureDefinition(174,
            "dls_raw_step_max_abs",
            "DLS raw step max abs",
            "Directional DLS preview",
            "Largest absolute raw preview joint increment.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "dls.rawJointStep.maxOfOrNull(::abs) ?: 0.0; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "rad/m mixed for mixed topology",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Largest absolute raw preview joint increment.",
            "Candidate input for testing whether dls raw step max abs helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[43]",
            "feature-dictionary-v1"),
        FeatureDefinition(175,
            "dls_normalized_step_l2",
            "DLS normalized step l2",
            "Directional DLS preview",
            "Euclidean norm of DLS increments divided by each half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "dls.normalizedStepNorm; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Euclidean norm of DLS increments divided by each half span.",
            "Candidate input for testing whether dls normalized step l2 helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[44]",
            "feature-dictionary-v1"),
        FeatureDefinition(176,
            "dls_normalized_step_rms",
            "DLS normalized step rms",
            "Directional DLS preview",
            "RMS of half-span-normalized DLS increments.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "rms(normalizedSteps); rms = stableNorm(values)/sqrt(max(number of values,1)); stableNorm folds hypot; normalizedSteps = dls.rawJointStep[j]/halfSpans[j]; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "RMS of half-span-normalized DLS increments.",
            "Candidate input for testing whether dls normalized step rms helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[45]",
            "feature-dictionary-v1"),
        FeatureDefinition(177,
            "dls_normalized_step_max_abs",
            "DLS normalized step max abs",
            "Directional DLS preview",
            "Maximum absolute half-span-normalized DLS increment.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "normalizedSteps.maxOfOrNull(::abs) ?: 0.0; normalizedSteps = dls.rawJointStep[j]/halfSpans[j]; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Maximum absolute half-span-normalized DLS increment.",
            "Candidate input for testing whether dls normalized step max abs helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[46]",
            "feature-dictionary-v1"),
        FeatureDefinition(178,
            "dls_max_step_scale",
            "DLS max step scale",
            "Directional DLS preview",
            "Uniform scalar reducing the normalized DLS step to maxStep when necessary.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "clippedScale; clippedScale = maxStep/dls.normalizedStepNorm if norm>maxStep and norm>EPS, else1; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","ikMaxStep","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Uniform scalar reducing the normalized DLS step to maxStep when necessary.",
            "Candidate input for testing whether dls max step scale helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[47]",
            "feature-dictionary-v1"),
        FeatureDefinition(179,
            "dls_predicted_cartesian_step",
            "DLS predicted cartesian step",
            "Directional DLS preview",
            "Norm of Cartesian displacement predicted by the linearly approximated clipped step.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "predictedStepNorm; predictedStepNorm = norm3(predictedCartesianStep); norm3 = hypot(hypot(x,y),z); predictedCartesianStep = kinematics.jacobian * clippedJointStep; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; clippedJointStep = dls.rawJointStep * clippedScale; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector; clippedScale = maxStep/dls.normalizedStepNorm if norm>maxStep and norm>EPS, else1",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","ikMaxStep","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Norm of Cartesian displacement predicted by the linearly approximated clipped step.",
            "Candidate input for testing whether dls predicted cartesian step helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[48]",
            "feature-dictionary-v1"),
        FeatureDefinition(180,
            "dls_predicted_residual",
            "DLS predicted residual",
            "Directional DLS preview",
            "Norm of residual predicted by the clipped linearized DLS step.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "predictedResidualNorm; predictedResidualNorm = norm3(predictedResidual); norm3 = hypot(hypot(x,y),z); predictedResidual = error - predictedCartesianStep; error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; predictedCartesianStep = kinematics.jacobian * clippedJointStep; clippedJointStep = dls.rawJointStep * clippedScale; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); clippedScale = maxStep/dls.normalizedStepNorm if norm>maxStep and norm>EPS, else1",
            "m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","ikMaxStep","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Norm of residual predicted by the clipped linearized DLS step.",
            "Candidate input for testing whether dls predicted residual helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[49]",
            "feature-dictionary-v1")
    )
    private fun block15():List<FeatureDefinition> = listOf(
        FeatureDefinition(181,
            "dls_predicted_residual_ratio",
            "DLS predicted residual ratio",
            "Directional DLS preview",
            "Predicted residual norm divided by initial error norm.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "predictedResidualNorm / max(errorNorm, EPS); predictedResidualNorm = norm3(predictedResidual); norm3 = hypot(hypot(x,y),z); predictedResidual = error - predictedCartesianStep; error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; predictedCartesianStep = kinematics.jacobian * clippedJointStep; clippedJointStep = dls.rawJointStep * clippedScale; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); clippedScale = maxStep/dls.normalizedStepNorm if norm>maxStep and norm>EPS, else1",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","ikMaxStep","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Predicted residual norm divided by initial error norm.",
            "Candidate input for testing whether dls predicted residual ratio helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[50]",
            "feature-dictionary-v1"),
        FeatureDefinition(182,
            "dls_predicted_improvement_ratio",
            "DLS predicted improvement ratio",
            "Directional DLS preview",
            "Fractional linearized error decrease after the clipped preview.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "(errorNorm - predictedResidualNorm) / max(errorNorm, EPS); errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; predictedResidualNorm = norm3(predictedResidual); norm3 = hypot(hypot(x,y),z); predictedResidual = error - predictedCartesianStep; predictedCartesianStep = kinematics.jacobian * clippedJointStep; clippedJointStep = dls.rawJointStep * clippedScale; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; clippedScale = maxStep/dls.normalizedStepNorm if norm>maxStep and norm>EPS, else1",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","ikMaxStep","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Fractional linearized error decrease after the clipped preview.",
            "Candidate input for testing whether dls predicted improvement ratio helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[51]",
            "feature-dictionary-v1"),
        FeatureDefinition(183,
            "dls_error_alignment_cosine",
            "DLS error alignment cosine",
            "Directional DLS preview",
            "Cosine alignment of the initial error and predicted Cartesian step.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "alignment; alignment = dot(error,predictedCartesianStep)/(errorNorm*predictedStepNorm) if both norms>EPS, else0; error = (targetX,targetY,targetZ) - kinematics.endEffector; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; predictedCartesianStep = kinematics.jacobian * clippedJointStep; clippedJointStep = dls.rawJointStep * clippedScale; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); clippedScale = maxStep/dls.normalizedStepNorm if norm>maxStep and norm>EPS, else1; predictedStepNorm = norm3(predictedCartesianStep); norm3 = hypot(hypot(x,y),z)",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","ikMaxStep","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Cosine alignment of the initial error and predicted Cartesian step.",
            "Candidate input for testing whether dls error alignment cosine helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[52]",
            "feature-dictionary-v1"),
        FeatureDefinition(184,
            "dls_preview_lambda",
            "DLS preview lambda",
            "Directional DLS preview",
            "Preview damping chosen from configured damping, singularity thresholds and initial error.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "lambda; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "m in the half-span-scaled preview",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Preview damping chosen from configured damping, singularity thresholds and initial error.",
            "Candidate input for testing whether dls preview lambda helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[53]",
            "feature-dictionary-v1"),
        FeatureDefinition(185,
            "dls_lambda_to_sigma_min",
            "DLS lambda to sigma min",
            "Directional DLS preview",
            "Preview damping relative to the minimum expected-rank singular value.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "lambda / max(sigmaExpectedMin, EPS); lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Preview damping relative to the minimum expected-rank singular value.",
            "Candidate input for testing whether dls lambda to sigma min helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[54]",
            "feature-dictionary-v1"),
        FeatureDefinition(186,
            "dls_lambda_to_sigma_max",
            "DLS lambda to sigma max",
            "Directional DLS preview",
            "Preview damping relative to the maximum singular value.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "lambda / max(sigmaMax, EPS); lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Preview damping relative to the maximum singular value.",
            "Candidate input for testing whether dls lambda to sigma max helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[55]",
            "feature-dictionary-v1"),
        FeatureDefinition(187,
            "dls_joint_effort_per_error",
            "DLS joint effort per error",
            "Directional DLS preview",
            "Normalized joint effort divided by initial Cartesian error.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "dls.normalizedStepNorm / max(errorNorm, EPS); dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "1/m",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Normalized joint effort divided by initial Cartesian error.",
            "Candidate input for testing whether dls joint effort per error helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[56]",
            "feature-dictionary-v1"),
        FeatureDefinition(188,
            "dls_min_directional_headroom",
            "DLS min directional headroom",
            "Directional DLS preview",
            "Smallest available joint travel to boundary divided by proposed raw step magnitude.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "directionalHeadrooms.minOrNull()?.coerceAtMost(HEADROOM_CAP) ?: 0.0; directionalHeadrooms = available travel toward sign(dls.rawJointStep[j]) divided by abs(step); available=maximums[j]-seeds[j] for step>=0 else seeds[j]-minimums[j]; return HEADROOM_CAP when abs(step)<=EPS; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector; HEADROOM_CAP = 1e6",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Smallest available joint travel to boundary divided by proposed raw step magnitude.",
            "Candidate input for testing whether dls min directional headroom helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[57]",
            "feature-dictionary-v1"),
        FeatureDefinition(189,
            "dls_mean_directional_headroom",
            "DLS mean directional headroom",
            "Directional DLS preview",
            "Average available travel-to-raw-step ratio.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "directionalHeadrooms.average().coerceAtMost(HEADROOM_CAP); directionalHeadrooms = available travel toward sign(dls.rawJointStep[j]) divided by abs(step); available=maximums[j]-seeds[j] for step>=0 else seeds[j]-minimums[j]; return HEADROOM_CAP when abs(step)<=EPS; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector; HEADROOM_CAP = 1e6",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Average available travel-to-raw-step ratio.",
            "Candidate input for testing whether dls mean directional headroom helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[58]",
            "feature-dictionary-v1"),
        FeatureDefinition(190,
            "dls_toward_nearer_limit_fraction",
            "DLS toward nearer limit fraction",
            "Directional DLS preview",
            "Fraction of raw DLS steps pointing toward the nearer joint limit.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "towardNearerLimit.average(); towardNearerLimit = 1 if dls.rawJointStep[j] points toward nearer margin min(lowerMargins[j],upperMargins[j]) (lower margin wins ties), otherwise0; averaged over present joints; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector; lowerMargins = (seeds[j]-minimums[j])/spans[j]; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Fraction of raw DLS steps pointing toward the nearer joint limit.",
            "Candidate input for testing whether dls toward nearer limit fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[59]",
            "feature-dictionary-v1"),
        FeatureDefinition(191,
            "dls_predicted_limit_violation_fraction",
            "DLS predicted limit violation fraction",
            "Directional DLS preview",
            "Fraction of raw increments exceeding a complete half span in absolute normalized magnitude.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "normalizedSteps.count { abs(it) > 1.0 }.toDouble() / count; normalizedSteps = dls.rawJointStep[j]/halfSpans[j]; dls = solve (gram + lambda^2 I) taskStep=error by pivoted3x3 elimination (diagonals <=EPS replaced by EPS); rawJointStep[j]=halfSpans[j]^2 * dot(kinematics.jacobian column j,taskStep); rawJointStepNorm=stableNorm(rawJointStep); normalizedStepNorm=stableNorm(rawJointStep[j]/halfSpans[j]); gram = J_scaled * transpose(J_scaled), J_scaled column j = kinematics.jacobian column j * halfSpans[j]; kinematics = standard-DH seed FK with jointTypes: for REVOLUTE active theta=seeds[j], d=d[j]; for PRISMATIC theta=theta[j], active d=seeds[j]. Accumulate R and p; local rotation uses active theta and alpha[j]: rows(cos(theta),-sin(theta)*cos(alpha),sin(theta)*sin(alpha)); (sin(theta),cos(theta)*cos(alpha),-cos(theta)*sin(alpha)); (0,sin(alpha),cos(alpha)). Local translation (a*cos(active theta),a*sin(active theta),active d). Jacobian columns: pre-joint z axis for PRISMATIC, z cross (endEffector - pre-joint origin) for REVOLUTE; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; lambda = max(damping*singularityMultiplier,errorNorm*0.5,EPS); singularityMultiplier = 10 if sigmaExpectedMin<=1e-5 or condition>=1e5; else3 if sigmaExpectedMin<=1e-3 or condition>=1e3; else1; sigmaExpectedMin = singularValues[expectedRank-1]; singularValues = descending sqrt(max(0,eigenvalue(gram))); symmetric Jacobi rotation solver, at most32 sweeps, off-diagonal convergence EPS; expectedRank = clamp(count,1,3); count = jointTypes.size; condition = sigmaMax / sigmaExpectedMin if sigmaExpectedMin > EPS, else CONDITION_CAP; sigmaMax = singularValues[0]; CONDITION_CAP = 1e12; errorNorm = hypot(hypot(error[0],error[1]),error[2]); error = (targetX,targetY,targetZ) - kinematics.endEffector",
            "dimensionless",
            listOf("dhAMeters","dhAlphaRad","dhDMeters","dhThetaRad","ikDamping","jointMaxValues","jointMinValues","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Fraction of raw increments exceeding a complete half span in absolute normalized magnitude.",
            "Candidate input for testing whether dls predicted limit violation fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "This is a single local linearized preview, not actual solver convergence or certified reachability. Half-span normalization removes rad/m mixture only for normalized steps. Damping and step clipping follow the documented preview, not a measured final update. Near-zero denominators use EPS1e-12; headroom is capped1e6. Despite the name, |dq/halfSpan|>1 is counted; it does NOT test actual crossing from the current seed to a limit. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[60]",
            "feature-dictionary-v1"),
        FeatureDefinition(192,
            "seed_margin_mean",
            "Seed margin mean",
            "Joint-limit pressure",
            "Mean nearest seed-to-limit margin normalized by full span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "nearestMargins.average(); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Mean nearest seed-to-limit margin normalized by full span.",
            "Candidate input for testing whether seed margin mean helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[61]",
            "feature-dictionary-v1")
    )
    private fun block16():List<FeatureDefinition> = listOf(
        FeatureDefinition(193,
            "seed_margin_standard_deviation",
            "Seed margin standard deviation",
            "Joint-limit pressure",
            "Population standard deviation of nearest seed margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "standardDeviation(nearestMargins); standardDeviation = population standard deviation: stableNorm(values-mean)/sqrt(number of values);0 if fewer than2; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Population standard deviation of nearest seed margins.",
            "Candidate input for testing whether seed margin standard deviation helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[62]",
            "feature-dictionary-v1"),
        FeatureDefinition(194,
            "seed_margin_min",
            "Seed margin min",
            "Joint-limit pressure",
            "Smallest nearest seed margin.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "nearestMargins.minOrNull() ?: 0.0; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Smallest nearest seed margin.",
            "Candidate input for testing whether seed margin min helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[63]",
            "feature-dictionary-v1"),
        FeatureDefinition(195,
            "seed_margin_max",
            "Seed margin max",
            "Joint-limit pressure",
            "Largest nearest seed margin.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "nearestMargins.maxOrNull() ?: 0.0; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Largest nearest seed margin.",
            "Candidate input for testing whether seed margin max helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[64]",
            "feature-dictionary-v1"),
        FeatureDefinition(196,
            "seed_margin_harmonic_mean",
            "Seed margin harmonic mean",
            "Joint-limit pressure",
            "Harmonic mean of nearest margins with a finite floor.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "harmonicMean(nearestMargins); harmonicMean = number of values / sum(1/max(value,LIMIT_EPS)); LIMIT_EPS = 1e-6; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Harmonic mean of nearest margins with a finite floor.",
            "Candidate input for testing whether seed margin harmonic mean helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[65]",
            "feature-dictionary-v1"),
        FeatureDefinition(197,
            "seed_limit_barrier_mean",
            "Seed limit barrier mean",
            "Joint-limit pressure",
            "Mean negative-log barrier of nearest joint margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "barriers.average(); barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Mean negative-log barrier of nearest joint margins.",
            "Candidate input for testing whether seed limit barrier mean helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[66]",
            "feature-dictionary-v1"),
        FeatureDefinition(198,
            "seed_limit_barrier_rms",
            "Seed limit barrier rms",
            "Joint-limit pressure",
            "RMS of negative-log joint barriers.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "rms(barriers); rms = stableNorm(values)/sqrt(max(number of values,1)); stableNorm folds hypot; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "RMS of negative-log joint barriers.",
            "Candidate input for testing whether seed limit barrier rms helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[67]",
            "feature-dictionary-v1"),
        FeatureDefinition(199,
            "seed_limit_barrier_max",
            "Seed limit barrier max",
            "Joint-limit pressure",
            "Largest negative-log joint barrier.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "barriers.maxOrNull() ?: 0.0; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Largest negative-log joint barrier.",
            "Candidate input for testing whether seed limit barrier max helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[68]",
            "feature-dictionary-v1"),
        FeatureDefinition(200,
            "seed_near_limit_fraction_05",
            "Seed near limit fraction 05",
            "Joint-limit pressure",
            "Fraction of joints with nearest margin at most5% of full span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "nearestMargins.count { it <= 0.05 }.toDouble() / count; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; count = jointTypes.size",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Fraction of joints with nearest margin at most5% of full span.",
            "Candidate input for testing whether seed near limit fraction 05 helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[69]",
            "feature-dictionary-v1"),
        FeatureDefinition(201,
            "seed_near_limit_fraction_10",
            "Seed near limit fraction 10",
            "Joint-limit pressure",
            "Fraction of joints with nearest margin at most10% of full span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "nearestMargins.count { it <= 0.10 }.toDouble() / count; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; count = jointTypes.size",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Fraction of joints with nearest margin at most10% of full span.",
            "Candidate input for testing whether seed near limit fraction 10 helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[70]",
            "feature-dictionary-v1"),
        FeatureDefinition(202,
            "seed_near_limit_fraction_20",
            "Seed near limit fraction 20",
            "Joint-limit pressure",
            "Fraction of joints with nearest margin at most20% of full span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "nearestMargins.count { it <= 0.20 }.toDouble() / count; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; count = jointTypes.size",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Fraction of joints with nearest margin at most20% of full span.",
            "Candidate input for testing whether seed near limit fraction 20 helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[71]",
            "feature-dictionary-v1"),
        FeatureDefinition(203,
            "seed_signed_position_mean",
            "Seed signed position mean",
            "Joint-limit pressure",
            "Mean signed seed position relative to interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "signedPositions.average(); signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Mean signed seed position relative to interval centre in half-span units.",
            "Candidate input for testing whether seed signed position mean helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[72]",
            "feature-dictionary-v1"),
        FeatureDefinition(204,
            "seed_signed_position_rms",
            "Seed signed position rms",
            "Joint-limit pressure",
            "RMS signed centre-relative seed positions.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "rms(signedPositions); rms = stableNorm(values)/sqrt(max(number of values,1)); stableNorm folds hypot; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "RMS signed centre-relative seed positions.",
            "Candidate input for testing whether seed signed position rms helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[73]",
            "feature-dictionary-v1")
    )
    private fun block17():List<FeatureDefinition> = listOf(
        FeatureDefinition(205,
            "seed_signed_position_max_abs",
            "Seed signed position max abs",
            "Joint-limit pressure",
            "Largest absolute half-span-normalized centre displacement.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "signedPositions.maxOfOrNull(::abs) ?: 0.0; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Largest absolute half-span-normalized centre displacement.",
            "Candidate input for testing whether seed signed position max abs helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[74]",
            "feature-dictionary-v1"),
        FeatureDefinition(206,
            "joint_span_mean_recomputed",
            "Joint span mean recomputed",
            "Joint-limit pressure",
            "Mean raw joint span recomputed from limits.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "spans.average(); spans = maximums[j] - minimums[j] for each present j",
            "rad/m mixed for mixed topology",
            listOf("jointMaxValues","jointMinValues"),
            "Mean raw joint span recomputed from limits.",
            "Candidate input for testing whether joint span mean recomputed helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[75]",
            "feature-dictionary-v1"),
        FeatureDefinition(207,
            "joint_span_standard_deviation",
            "Joint span standard deviation",
            "Joint-limit pressure",
            "Population standard deviation of raw coordinate spans.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "standardDeviation(spans); standardDeviation = population standard deviation: stableNorm(values-mean)/sqrt(number of values);0 if fewer than2; spans = maximums[j] - minimums[j] for each present j",
            "rad/m mixed for mixed topology",
            listOf("jointMaxValues","jointMinValues"),
            "Population standard deviation of raw coordinate spans.",
            "Candidate input for testing whether joint span standard deviation helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[76]",
            "feature-dictionary-v1"),
        FeatureDefinition(208,
            "joint_span_max",
            "Joint span max",
            "Joint-limit pressure",
            "Largest raw coordinate span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "spans.maxOrNull() ?: 0.0; spans = maximums[j] - minimums[j] for each present j",
            "rad/m mixed for mixed topology",
            listOf("jointMaxValues","jointMinValues"),
            "Largest raw coordinate span.",
            "Candidate input for testing whether joint span max helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[77]",
            "feature-dictionary-v1"),
        FeatureDefinition(209,
            "joint_span_condition_ratio",
            "Joint span condition ratio",
            "Joint-limit pressure",
            "Ratio of largest to smallest raw span, with denominator floor.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "(spans.maxOrNull() ?: 0.0) / max(spans.minOrNull() ?: 0.0, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "numeric ratio; mixed rad/m spans when topology mixed",
            listOf("jointMaxValues","jointMinValues"),
            "Ratio of largest to smallest raw span, with denominator floor.",
            "Candidate input for testing whether joint span condition ratio helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Computed over present joints only, not padded slots. Margins use full spans; signed positions use half spans (floor1e-12). Log/harmonic margin floor1e-6 prevents infinity. Mixed raw spans combine metres and radians. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[78]",
            "feature-dictionary-v1"),
        FeatureDefinition(210,
            "total_link_extent",
            "Total link extent",
            "Chain geometry",
            "Sum of static DH translation extents, floored for finite division.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "totalExtent; totalExtent = max(sum(staticExtents),EPS); staticExtents = hypot(a[j],d[j]); EPS = 1e-12",
            "m",
            listOf("dhAMeters","dhDMeters"),
            "Sum of static DH translation extents, floored for finite division.",
            "Candidate input for testing whether total link extent helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[79]",
            "feature-dictionary-v1"),
        FeatureDefinition(211,
            "maximum_link_extent",
            "Maximum link extent",
            "Chain geometry",
            "Largest static DH translation extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "maxExtent; maxExtent = max(staticExtents); staticExtents = hypot(a[j],d[j])",
            "m",
            listOf("dhAMeters","dhDMeters"),
            "Largest static DH translation extent.",
            "Candidate input for testing whether maximum link extent helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[80]",
            "feature-dictionary-v1"),
        FeatureDefinition(212,
            "minimum_link_extent",
            "Minimum link extent",
            "Chain geometry",
            "Smallest static DH translation extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "minExtent; minExtent = min(staticExtents); staticExtents = hypot(a[j],d[j])",
            "m",
            listOf("dhAMeters","dhDMeters"),
            "Smallest static DH translation extent.",
            "Candidate input for testing whether minimum link extent helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[81]",
            "feature-dictionary-v1"),
        FeatureDefinition(213,
            "link_extent_std_recomputed",
            "Link extent std recomputed",
            "Chain geometry",
            "Population standard deviation of static DH extents.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "standardDeviation(staticExtents); standardDeviation = population standard deviation: stableNorm(values-mean)/sqrt(number of values);0 if fewer than2; staticExtents = hypot(a[j],d[j])",
            "m",
            listOf("dhAMeters","dhDMeters"),
            "Population standard deviation of static DH extents.",
            "Candidate input for testing whether link extent std recomputed helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[82]",
            "feature-dictionary-v1"),
        FeatureDefinition(214,
            "link_extent_coefficient_of_variation",
            "Link extent coefficient of variation",
            "Chain geometry",
            "Static extent standard deviation divided by mean.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "standardDeviation(staticExtents) / max(staticExtents.average(), EPS); standardDeviation = population standard deviation: stableNorm(values-mean)/sqrt(number of values);0 if fewer than2; staticExtents = hypot(a[j],d[j]); EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters"),
            "Static extent standard deviation divided by mean.",
            "Candidate input for testing whether link extent coefficient of variation helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[83]",
            "feature-dictionary-v1"),
        FeatureDefinition(215,
            "dominant_link_fraction",
            "Dominant link fraction",
            "Chain geometry",
            "Largest static extent as a fraction of their sum.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "maxExtent / totalExtent; maxExtent = max(staticExtents); staticExtents = hypot(a[j],d[j]); totalExtent = max(sum(staticExtents),EPS); EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters"),
            "Largest static extent as a fraction of their sum.",
            "Candidate input for testing whether dominant link fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[84]",
            "feature-dictionary-v1"),
        FeatureDefinition(216,
            "link_extent_entropy",
            "Link extent entropy",
            "Chain geometry",
            "Shannon entropy of static extent shares.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "extentEntropy; extentEntropy = -sum(share*ln(share) for extentShares>EPS); extentShares = staticExtents/totalExtent; staticExtents = hypot(a[j],d[j]); totalExtent = max(sum(staticExtents),EPS); EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters"),
            "Shannon entropy of static extent shares.",
            "Candidate input for testing whether link extent entropy helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[85]",
            "feature-dictionary-v1")
    )
    private fun block18():List<FeatureDefinition> = listOf(
        FeatureDefinition(217,
            "effective_link_count",
            "Effective link count",
            "Chain geometry",
            "Exponential extent-share entropy as effective link count.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "exp(extentEntropy); extentEntropy = -sum(share*ln(share) for extentShares>EPS); extentShares = staticExtents/totalExtent; staticExtents = hypot(a[j],d[j]); totalExtent = max(sum(staticExtents),EPS); EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters"),
            "Exponential extent-share entropy as effective link count.",
            "Candidate input for testing whether effective link count helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[86]",
            "feature-dictionary-v1"),
        FeatureDefinition(218,
            "inner_radial_bound_proxy",
            "Inner radial bound proxy",
            "Chain geometry",
            "Triangle-inequality inner radial void proxy based on the dominant static extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "innerRadialBound; innerRadialBound = max(0,maxExtent-(totalExtent-maxExtent)); maxExtent = max(staticExtents); staticExtents = hypot(a[j],d[j]); totalExtent = max(sum(staticExtents),EPS); EPS = 1e-12",
            "m",
            listOf("dhAMeters","dhDMeters"),
            "Triangle-inequality inner radial void proxy based on the dominant static extent.",
            "Candidate input for testing whether inner radial bound proxy helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[87]",
            "feature-dictionary-v1"),
        FeatureDefinition(219,
            "normalized_inner_radial_bound",
            "Normalized inner radial bound",
            "Chain geometry",
            "Inner radial proxy divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "innerRadialBound / reach; innerRadialBound = max(0,maxExtent-(totalExtent-maxExtent)); maxExtent = max(staticExtents); staticExtents = hypot(a[j],d[j]); totalExtent = max(sum(staticExtents),EPS); EPS = 1e-12; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes"),
            "Inner radial proxy divided by conservative extent.",
            "Candidate input for testing whether normalized inner radial bound helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[88]",
            "feature-dictionary-v1"),
        FeatureDefinition(220,
            "normalized_inner_boundary_gap",
            "Normalized inner boundary gap",
            "Chain geometry",
            "Signed target-radius distance above the inner proxy, normalized by extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "(targetRadius - innerRadialBound) / reach; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z); innerRadialBound = max(0,maxExtent-(totalExtent-maxExtent)); maxExtent = max(staticExtents); staticExtents = hypot(a[j],d[j]); totalExtent = max(sum(staticExtents),EPS); EPS = 1e-12; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","targetX","targetY","targetZ"),
            "Signed target-radius distance above the inner proxy, normalized by extent.",
            "Candidate input for testing whether normalized inner boundary gap helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[89]",
            "feature-dictionary-v1"),
        FeatureDefinition(221,
            "normalized_outer_boundary_gap",
            "Normalized outer boundary gap",
            "Chain geometry",
            "Signed conservative outer-radius headroom above target radius.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "(reach - targetRadius) / reach; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z)",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","targetX","targetY","targetZ"),
            "Signed conservative outer-radius headroom above target radius.",
            "Candidate input for testing whether normalized outer boundary gap helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[90]",
            "feature-dictionary-v1"),
        FeatureDefinition(222,
            "normalized_nearest_radial_boundary_gap",
            "Normalized nearest radial boundary gap",
            "Chain geometry",
            "Nearest absolute radial distance to the inner/outer proxies in extent units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "min(abs(targetRadius - innerRadialBound), abs(reach - targetRadius)) / reach; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z); innerRadialBound = max(0,maxExtent-(totalExtent-maxExtent)); maxExtent = max(staticExtents); staticExtents = hypot(a[j],d[j]); totalExtent = max(sum(staticExtents),EPS); EPS = 1e-12; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","targetX","targetY","targetZ"),
            "Nearest absolute radial distance to the inner/outer proxies in extent units.",
            "Candidate input for testing whether normalized nearest radial boundary gap helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[91]",
            "feature-dictionary-v1"),
        FeatureDefinition(223,
            "total_absolute_dh_a",
            "Total absolute dh a",
            "Chain geometry",
            "Sum of absolute standard-DH a parameters.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "totalAbsA; totalAbsA = sum(abs(a))",
            "m",
            listOf("dhAMeters"),
            "Sum of absolute standard-DH a parameters.",
            "Candidate input for testing whether total absolute dh a helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[92]",
            "feature-dictionary-v1"),
        FeatureDefinition(224,
            "total_absolute_dh_d",
            "Total absolute dh d",
            "Chain geometry",
            "Sum of absolute static standard-DH d parameters.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "totalAbsD; totalAbsD = sum(abs(d))",
            "m",
            listOf("dhDMeters"),
            "Sum of absolute static standard-DH d parameters.",
            "Candidate input for testing whether total absolute dh d helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[93]",
            "feature-dictionary-v1"),
        FeatureDefinition(225,
            "dh_a_extent_fraction",
            "DH a extent fraction",
            "Chain geometry",
            "Fraction of total absolute a+d contributed by a.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "totalAbsA / max(totalAbsA + totalAbsD, EPS); totalAbsA = sum(abs(a)); totalAbsD = sum(abs(d)); EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters"),
            "Fraction of total absolute a+d contributed by a.",
            "Candidate input for testing whether dh a extent fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[94]",
            "feature-dictionary-v1"),
        FeatureDefinition(226,
            "mean_dh_alpha_sin",
            "Mean dh alpha sin",
            "Chain geometry",
            "Mean sine of static DH twist angles.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "alphaSin.average(); alphaSin = sin(alpha[j])",
            "dimensionless",
            listOf("dhAlphaRad"),
            "Mean sine of static DH twist angles.",
            "Candidate input for testing whether mean dh alpha sin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[95]",
            "feature-dictionary-v1"),
        FeatureDefinition(227,
            "mean_dh_alpha_cos",
            "Mean dh alpha cos",
            "Chain geometry",
            "Mean cosine of static DH twist angles.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "alphaCos.average(); alphaCos = cos(alpha[j])",
            "dimensionless",
            listOf("dhAlphaRad"),
            "Mean cosine of static DH twist angles.",
            "Candidate input for testing whether mean dh alpha cos helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[96]",
            "feature-dictionary-v1"),
        FeatureDefinition(228,
            "dh_alpha_circular_concentration",
            "DH alpha circular concentration",
            "Chain geometry",
            "Resultant length of mean twist sine/cosine: circular concentration.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "hypot(alphaSin.average(), alphaCos.average()); alphaSin = sin(alpha[j]); alphaCos = cos(alpha[j])",
            "dimensionless",
            listOf("dhAlphaRad"),
            "Resultant length of mean twist sine/cosine: circular concentration.",
            "Candidate input for testing whether dh alpha circular concentration helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[97]",
            "feature-dictionary-v1")
    )
    private fun block19():List<FeatureDefinition> = listOf(
        FeatureDefinition(229,
            "nonzero_twist_fraction",
            "Nonzero twist fraction",
            "Chain geometry",
            "Fraction of twists with absolute angle greater than1e-6 radians.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "input.alpha.count { abs(it) > 1e-6 }.toDouble() / count; count = jointTypes.size",
            "dimensionless",
            listOf("dhAlphaRad","jointTypes"),
            "Fraction of twists with absolute angle greater than1e-6 radians.",
            "Candidate input for testing whether nonzero twist fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[98]",
            "feature-dictionary-v1"),
        FeatureDefinition(230,
            "redundant_dof_fraction",
            "Redundant dof fraction",
            "Chain geometry",
            "Count of joints beyond3 divided by actual count.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "max(0, count - 3).toDouble() / max(count, 1); count = jointTypes.size",
            "dimensionless",
            listOf("jointTypes"),
            "Count of joints beyond3 divided by actual count.",
            "Candidate input for testing whether redundant dof fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[99]",
            "feature-dictionary-v1"),
        FeatureDefinition(231,
            "task_dof_ratio",
            "Task dof ratio",
            "Chain geometry",
            "Joint count divided by the3 positional task coordinates.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "count.toDouble() / 3.0; count = jointTypes.size",
            "dimensionless",
            listOf("jointTypes"),
            "Joint count divided by the3 positional task coordinates.",
            "Candidate input for testing whether task dof ratio helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[100]",
            "feature-dictionary-v1"),
        FeatureDefinition(232,
            "joint_type_transition_fraction",
            "Joint type transition fraction",
            "Chain geometry",
            "Fraction of adjacent joints whose revolute/prismatic type changes.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "typeTransitions.toDouble() / max(count - 1, 1); typeTransitions = number of adjacent unequal prismaticFlags; prismaticFlags = 1 for jointTypes[j] PRISMATIC else0; count = jointTypes.size",
            "dimensionless",
            listOf("jointTypes"),
            "Fraction of adjacent joints whose revolute/prismatic type changes.",
            "Candidate input for testing whether joint type transition fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[101]",
            "feature-dictionary-v1"),
        FeatureDefinition(233,
            "longest_joint_type_run_fraction",
            "Longest joint type run fraction",
            "Chain geometry",
            "Longest consecutive run of equal joint type divided by count.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "longestTypeRun.toDouble() / count; longestTypeRun = longest consecutive run of equal prismaticFlags; prismaticFlags = 1 for jointTypes[j] PRISMATIC else0; count = jointTypes.size",
            "dimensionless",
            listOf("jointTypes"),
            "Longest consecutive run of equal joint type divided by count.",
            "Candidate input for testing whether longest joint type run fraction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "These are static-chain descriptors or radial proxies, not a certified workspace, independence/rank claim or manufacturer specification. Static DH d excludes active prismatic displacement. Extent denominator floor1e-12; zero extents produce entropy0/effective count1 by convention. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · GLOBAL_FEATURE_NAMES[102]",
            "feature-dictionary-v1"),
        FeatureDefinition(234,
            "joint_1_seed_signed_position",
            "Joint 1 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 1: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 1 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(235,
            "joint_1_seed_lower_margin",
            "Joint 1 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 1: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 1 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(236,
            "joint_1_seed_upper_margin",
            "Joint 1 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 1: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 1 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(237,
            "joint_1_seed_nearest_margin",
            "Joint 1 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 1: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 1 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(238,
            "joint_1_seed_limit_barrier",
            "Joint 1 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 1: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 1 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(239,
            "joint_1_home_signed_position",
            "Joint 1 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 1: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 1th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 1 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(240,
            "joint_1_seed_home_delta_normalized",
            "Joint 1 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 1: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 1 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1")
    )
    private fun block20():List<FeatureDefinition> = listOf(
        FeatureDefinition(241,
            "joint_1_seed_sin",
            "Joint 1 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 1: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 1 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(242,
            "joint_1_seed_cos",
            "Joint 1 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 1: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 1 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(243,
            "joint_1_static_extent_ratio",
            "Joint 1 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 1: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 1th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 1 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(244,
            "joint_1_active_extent_ratio",
            "Joint 1 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 1: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 1th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 1 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(245,
            "joint_1_dh_alpha_sin",
            "Joint 1 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 1: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 1th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 1 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(246,
            "joint_1_dh_alpha_cos",
            "Joint 1 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 1: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 1th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 1 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1"),
        FeatureDefinition(247,
            "joint_1_active_theta_sin",
            "Joint 1 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 1: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 1 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(248,
            "joint_1_active_theta_cos",
            "Joint 1 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 1: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 1th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 1 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥1; otherwise exactly0 in all15 expanded slots. Check baseline joint_1_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(249,
            "joint_2_seed_signed_position",
            "Joint 2 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 2: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 2 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(250,
            "joint_2_seed_lower_margin",
            "Joint 2 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 2: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 2 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(251,
            "joint_2_seed_upper_margin",
            "Joint 2 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 2: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 2 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(252,
            "joint_2_seed_nearest_margin",
            "Joint 2 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 2: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 2 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1")
    )
    private fun block21():List<FeatureDefinition> = listOf(
        FeatureDefinition(253,
            "joint_2_seed_limit_barrier",
            "Joint 2 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 2: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 2 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(254,
            "joint_2_home_signed_position",
            "Joint 2 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 2: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 2th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 2 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(255,
            "joint_2_seed_home_delta_normalized",
            "Joint 2 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 2: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 2 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(256,
            "joint_2_seed_sin",
            "Joint 2 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 2: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 2 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(257,
            "joint_2_seed_cos",
            "Joint 2 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 2: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 2 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(258,
            "joint_2_static_extent_ratio",
            "Joint 2 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 2: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 2th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 2 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(259,
            "joint_2_active_extent_ratio",
            "Joint 2 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 2: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 2th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 2 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(260,
            "joint_2_dh_alpha_sin",
            "Joint 2 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 2: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 2th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 2 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(261,
            "joint_2_dh_alpha_cos",
            "Joint 2 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 2: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 2th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 2 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1"),
        FeatureDefinition(262,
            "joint_2_active_theta_sin",
            "Joint 2 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 2: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 2 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(263,
            "joint_2_active_theta_cos",
            "Joint 2 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 2: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 2th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 2 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥2; otherwise exactly0 in all15 expanded slots. Check baseline joint_2_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(264,
            "joint_3_seed_signed_position",
            "Joint 3 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 3: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 3 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1")
    )
    private fun block22():List<FeatureDefinition> = listOf(
        FeatureDefinition(265,
            "joint_3_seed_lower_margin",
            "Joint 3 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 3: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 3 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(266,
            "joint_3_seed_upper_margin",
            "Joint 3 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 3: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 3 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(267,
            "joint_3_seed_nearest_margin",
            "Joint 3 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 3: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 3 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(268,
            "joint_3_seed_limit_barrier",
            "Joint 3 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 3: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 3 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(269,
            "joint_3_home_signed_position",
            "Joint 3 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 3: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 3th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 3 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(270,
            "joint_3_seed_home_delta_normalized",
            "Joint 3 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 3: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 3 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(271,
            "joint_3_seed_sin",
            "Joint 3 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 3: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 3 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(272,
            "joint_3_seed_cos",
            "Joint 3 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 3: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 3 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(273,
            "joint_3_static_extent_ratio",
            "Joint 3 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 3: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 3th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 3 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(274,
            "joint_3_active_extent_ratio",
            "Joint 3 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 3: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 3th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 3 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(275,
            "joint_3_dh_alpha_sin",
            "Joint 3 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 3: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 3th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 3 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(276,
            "joint_3_dh_alpha_cos",
            "Joint 3 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 3: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 3th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 3 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1")
    )
    private fun block23():List<FeatureDefinition> = listOf(
        FeatureDefinition(277,
            "joint_3_active_theta_sin",
            "Joint 3 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 3: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 3 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(278,
            "joint_3_active_theta_cos",
            "Joint 3 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 3: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 3th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 3 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥3; otherwise exactly0 in all15 expanded slots. Check baseline joint_3_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(279,
            "joint_4_seed_signed_position",
            "Joint 4 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 4: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 4 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(280,
            "joint_4_seed_lower_margin",
            "Joint 4 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 4: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 4 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(281,
            "joint_4_seed_upper_margin",
            "Joint 4 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 4: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 4 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(282,
            "joint_4_seed_nearest_margin",
            "Joint 4 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 4: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 4 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(283,
            "joint_4_seed_limit_barrier",
            "Joint 4 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 4: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 4 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(284,
            "joint_4_home_signed_position",
            "Joint 4 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 4: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 4th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 4 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(285,
            "joint_4_seed_home_delta_normalized",
            "Joint 4 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 4: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 4 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(286,
            "joint_4_seed_sin",
            "Joint 4 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 4: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 4 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(287,
            "joint_4_seed_cos",
            "Joint 4 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 4: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 4 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(288,
            "joint_4_static_extent_ratio",
            "Joint 4 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 4: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 4th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 4 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1")
    )
    private fun block24():List<FeatureDefinition> = listOf(
        FeatureDefinition(289,
            "joint_4_active_extent_ratio",
            "Joint 4 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 4: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 4th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 4 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(290,
            "joint_4_dh_alpha_sin",
            "Joint 4 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 4: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 4th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 4 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(291,
            "joint_4_dh_alpha_cos",
            "Joint 4 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 4: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 4th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 4 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1"),
        FeatureDefinition(292,
            "joint_4_active_theta_sin",
            "Joint 4 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 4: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 4 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(293,
            "joint_4_active_theta_cos",
            "Joint 4 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 4: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 4th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 4 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥4; otherwise exactly0 in all15 expanded slots. Check baseline joint_4_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(294,
            "joint_5_seed_signed_position",
            "Joint 5 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 5: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 5 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(295,
            "joint_5_seed_lower_margin",
            "Joint 5 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 5: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 5 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(296,
            "joint_5_seed_upper_margin",
            "Joint 5 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 5: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 5 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(297,
            "joint_5_seed_nearest_margin",
            "Joint 5 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 5: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 5 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(298,
            "joint_5_seed_limit_barrier",
            "Joint 5 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 5: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 5 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(299,
            "joint_5_home_signed_position",
            "Joint 5 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 5: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 5th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 5 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(300,
            "joint_5_seed_home_delta_normalized",
            "Joint 5 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 5: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 5 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1")
    )
    private fun block25():List<FeatureDefinition> = listOf(
        FeatureDefinition(301,
            "joint_5_seed_sin",
            "Joint 5 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 5: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 5 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(302,
            "joint_5_seed_cos",
            "Joint 5 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 5: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 5 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(303,
            "joint_5_static_extent_ratio",
            "Joint 5 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 5: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 5th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 5 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(304,
            "joint_5_active_extent_ratio",
            "Joint 5 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 5: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 5th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 5 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(305,
            "joint_5_dh_alpha_sin",
            "Joint 5 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 5: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 5th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 5 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(306,
            "joint_5_dh_alpha_cos",
            "Joint 5 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 5: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 5th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 5 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1"),
        FeatureDefinition(307,
            "joint_5_active_theta_sin",
            "Joint 5 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 5: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 5 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(308,
            "joint_5_active_theta_cos",
            "Joint 5 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 5: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 5th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 5 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥5; otherwise exactly0 in all15 expanded slots. Check baseline joint_5_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(309,
            "joint_6_seed_signed_position",
            "Joint 6 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 6: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 6 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(310,
            "joint_6_seed_lower_margin",
            "Joint 6 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 6: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 6 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(311,
            "joint_6_seed_upper_margin",
            "Joint 6 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 6: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 6 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(312,
            "joint_6_seed_nearest_margin",
            "Joint 6 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 6: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 6 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1")
    )
    private fun block26():List<FeatureDefinition> = listOf(
        FeatureDefinition(313,
            "joint_6_seed_limit_barrier",
            "Joint 6 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 6: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 6 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(314,
            "joint_6_home_signed_position",
            "Joint 6 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 6: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 6th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 6 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(315,
            "joint_6_seed_home_delta_normalized",
            "Joint 6 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 6: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 6 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(316,
            "joint_6_seed_sin",
            "Joint 6 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 6: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 6 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(317,
            "joint_6_seed_cos",
            "Joint 6 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 6: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 6 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(318,
            "joint_6_static_extent_ratio",
            "Joint 6 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 6: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 6th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 6 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(319,
            "joint_6_active_extent_ratio",
            "Joint 6 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 6: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 6th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 6 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(320,
            "joint_6_dh_alpha_sin",
            "Joint 6 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 6: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 6th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 6 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(321,
            "joint_6_dh_alpha_cos",
            "Joint 6 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 6: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 6th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 6 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1"),
        FeatureDefinition(322,
            "joint_6_active_theta_sin",
            "Joint 6 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 6: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 6 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(323,
            "joint_6_active_theta_cos",
            "Joint 6 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 6: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 6th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 6 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥6; otherwise exactly0 in all15 expanded slots. Check baseline joint_6_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(324,
            "joint_7_seed_signed_position",
            "Joint 7 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 7: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 7 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1")
    )
    private fun block27():List<FeatureDefinition> = listOf(
        FeatureDefinition(325,
            "joint_7_seed_lower_margin",
            "Joint 7 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 7: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 7 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(326,
            "joint_7_seed_upper_margin",
            "Joint 7 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 7: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 7 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(327,
            "joint_7_seed_nearest_margin",
            "Joint 7 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 7: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 7 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(328,
            "joint_7_seed_limit_barrier",
            "Joint 7 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 7: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 7 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(329,
            "joint_7_home_signed_position",
            "Joint 7 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 7: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 7th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 7 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(330,
            "joint_7_seed_home_delta_normalized",
            "Joint 7 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 7: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 7 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(331,
            "joint_7_seed_sin",
            "Joint 7 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 7: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 7 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(332,
            "joint_7_seed_cos",
            "Joint 7 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 7: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 7 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(333,
            "joint_7_static_extent_ratio",
            "Joint 7 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 7: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 7th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 7 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(334,
            "joint_7_active_extent_ratio",
            "Joint 7 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 7: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 7th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 7 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(335,
            "joint_7_dh_alpha_sin",
            "Joint 7 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 7: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 7th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 7 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(336,
            "joint_7_dh_alpha_cos",
            "Joint 7 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 7: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 7th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 7 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1")
    )
    private fun block28():List<FeatureDefinition> = listOf(
        FeatureDefinition(337,
            "joint_7_active_theta_sin",
            "Joint 7 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 7: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 7 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(338,
            "joint_7_active_theta_cos",
            "Joint 7 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 7: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 7th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 7 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥7; otherwise exactly0 in all15 expanded slots. Check baseline joint_7_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(339,
            "joint_8_seed_signed_position",
            "Joint 8 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 8: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 8 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(340,
            "joint_8_seed_lower_margin",
            "Joint 8 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 8: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 8 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(341,
            "joint_8_seed_upper_margin",
            "Joint 8 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 8: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 8 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(342,
            "joint_8_seed_nearest_margin",
            "Joint 8 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 8: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 8 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(343,
            "joint_8_seed_limit_barrier",
            "Joint 8 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 8: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 8 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(344,
            "joint_8_home_signed_position",
            "Joint 8 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 8: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 8th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 8 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(345,
            "joint_8_seed_home_delta_normalized",
            "Joint 8 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 8: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 8 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(346,
            "joint_8_seed_sin",
            "Joint 8 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 8: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 8 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(347,
            "joint_8_seed_cos",
            "Joint 8 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 8: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 8 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(348,
            "joint_8_static_extent_ratio",
            "Joint 8 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 8: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 8th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 8 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1")
    )
    private fun block29():List<FeatureDefinition> = listOf(
        FeatureDefinition(349,
            "joint_8_active_extent_ratio",
            "Joint 8 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 8: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 8th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 8 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(350,
            "joint_8_dh_alpha_sin",
            "Joint 8 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 8: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 8th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 8 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(351,
            "joint_8_dh_alpha_cos",
            "Joint 8 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 8: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 8th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 8 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1"),
        FeatureDefinition(352,
            "joint_8_active_theta_sin",
            "Joint 8 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 8: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 8 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(353,
            "joint_8_active_theta_cos",
            "Joint 8 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 8: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 8th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 8 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥8; otherwise exactly0 in all15 expanded slots. Check baseline joint_8_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(354,
            "joint_9_seed_signed_position",
            "Joint 9 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 9: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 9 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(355,
            "joint_9_seed_lower_margin",
            "Joint 9 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 9: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 9 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(356,
            "joint_9_seed_upper_margin",
            "Joint 9 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 9: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 9 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(357,
            "joint_9_seed_nearest_margin",
            "Joint 9 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 9: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 9 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(358,
            "joint_9_seed_limit_barrier",
            "Joint 9 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 9: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 9 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(359,
            "joint_9_home_signed_position",
            "Joint 9 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 9: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 9th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 9 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(360,
            "joint_9_seed_home_delta_normalized",
            "Joint 9 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 9: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 9 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1")
    )
    private fun block30():List<FeatureDefinition> = listOf(
        FeatureDefinition(361,
            "joint_9_seed_sin",
            "Joint 9 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 9: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 9 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(362,
            "joint_9_seed_cos",
            "Joint 9 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 9: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 9 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(363,
            "joint_9_static_extent_ratio",
            "Joint 9 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 9: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 9th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 9 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(364,
            "joint_9_active_extent_ratio",
            "Joint 9 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 9: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 9th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 9 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(365,
            "joint_9_dh_alpha_sin",
            "Joint 9 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 9: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 9th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 9 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(366,
            "joint_9_dh_alpha_cos",
            "Joint 9 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 9: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 9th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 9 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1"),
        FeatureDefinition(367,
            "joint_9_active_theta_sin",
            "Joint 9 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 9: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 9 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(368,
            "joint_9_active_theta_cos",
            "Joint 9 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 9: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 9th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 9 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥9; otherwise exactly0 in all15 expanded slots. Check baseline joint_9_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(369,
            "joint_10_seed_signed_position",
            "Joint 10 seed position relative to centre",
            "Per-joint transforms",
            "Ordered joint 10: Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; signedPositions[index]; signedPositions = (seeds[j]-centers[j])/halfSpans[j]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Signed displacement of the initial coordinate from the interval centre in half-span units.",
            "Candidate input for testing whether joint 10 seed position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "-1 at lower limit,+1 at upper; zero at centre. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(370,
            "joint_10_seed_lower_margin",
            "Joint 10 lower-limit margin",
            "Per-joint transforms",
            "Ordered joint 10: Initial coordinate minus minimum, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; lowerMargins[index]; lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Initial coordinate minus minimum, divided by full allowed span.",
            "Candidate input for testing whether joint 10 lower-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at lower limit,1 at upper; not a distance in metres/radians. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(371,
            "joint_10_seed_upper_margin",
            "Joint 10 upper-limit margin",
            "Per-joint transforms",
            "Ordered joint 10: Maximum minus initial coordinate, divided by full allowed span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; upperMargins[index]; upperMargins = (maximums[j]-seeds[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Maximum minus initial coordinate, divided by full allowed span.",
            "Candidate input for testing whether joint 10 upper-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at lower limit,0 at upper; complementary to lower margin. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(372,
            "joint_10_seed_nearest_margin",
            "Joint 10 nearest-limit margin",
            "Per-joint transforms",
            "Ordered joint 10: Minimum of lower and upper normalized margins.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; nearestMargins[index]; nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Minimum of lower and upper normalized margins.",
            "Candidate input for testing whether joint 10 nearest-limit margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "0 at either boundary,0.5 at centre; loses which side is nearer. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[3]",
            "feature-dictionary-v1")
    )
    private fun block31():List<FeatureDefinition> = listOf(
        FeatureDefinition(373,
            "joint_10_seed_limit_barrier",
            "Joint 10 seed limit barrier",
            "Per-joint transforms",
            "Ordered joint 10: Negative natural log of nearest margin floored at1e-6.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; barriers[index]; barriers = -ln(max(nearestMargins[j],LIMIT_EPS)); nearestMargins = min(lowerMargins[j],upperMargins[j]); lowerMargins = (seeds[j]-minimums[j])/spans[j]; spans = maximums[j] - minimums[j] for each present j; upperMargins = (maximums[j]-seeds[j])/spans[j]; LIMIT_EPS = 1e-6",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Negative natural log of nearest margin floored at1e-6.",
            "Candidate input for testing whether joint 10 seed limit barrier helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Saturates at -ln(1e-6) at a boundary; a finite modelling barrier, not physical force. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(374,
            "joint_10_home_signed_position",
            "Joint 10 home position relative to centre",
            "Per-joint transforms",
            "Ordered joint 10: Declared home offset from interval centre divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; normalizedHome; normalizedHome = (homes[index]-centers[index])/halfSpans[index]; centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","jointCount"),
            "This input describes the 10th joint only. Declared home offset from interval centre divided by half span.",
            "Candidate input for testing whether joint 10 home position relative to centre helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Describes home, not current or solved state. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(375,
            "joint_10_seed_home_delta_normalized",
            "Joint 10 seed minus home in half-span units",
            "Per-joint transforms",
            "Ordered joint 10: Initial seed displacement from home divided by half span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; normalizedSeedHome; normalizedSeedHome = (seeds[index]-homes[index])/halfSpans[index]; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Initial seed displacement from home divided by half span.",
            "Candidate input for testing whether joint 10 seed minus home in half-span units helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Twice the full-span offset used in context130 global seed-home statistics. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(376,
            "joint_10_seed_sin",
            "Joint 10 sine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 10: Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; if (revolute) sin(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Sine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 10 sine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Prismatic0 is a type sentinel, not sine of a translation. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(377,
            "joint_10_seed_cos",
            "Joint 10 cosine of revolute seed angle",
            "Per-joint transforms",
            "Ordered joint 10: Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; if (revolute) cos(input.seeds[index]) else 0.0; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("jointTypes","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Cosine of initial coordinate for a revolute joint;0 for prismatic.",
            "Candidate input for testing whether joint 10 cosine of revolute seed angle helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Both prismatic and missing slot return0, not cos(0)=1. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(378,
            "joint_10_static_extent_ratio",
            "Joint 10 static extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 10: hypot(a,d) for this static DH row divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; staticExtents[index] / reach; staticExtents = hypot(a[j],d[j]); reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","jointCount"),
            "This input describes the 10th joint only. hypot(a,d) for this static DH row divided by conservative extent.",
            "Candidate input for testing whether joint 10 static extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Static prismatic d is0; current extension is absent. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(379,
            "joint_10_active_extent_ratio",
            "Joint 10 active extent divided by reach",
            "Per-joint transforms",
            "Ordered joint 10: hypot(a,active d) at seed divided by conservative extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; activeExtents[index] / reach; activeExtents = hypot(a[j],if PRISMATIC then seeds[j] else d[j]) using jointTypes[j]; reach = sum_j hypot(a[j], if jointTypes[j] is PRISMATIC then max(abs(minimums[j]),abs(maximums[j])) else abs(d[j])); floored at EPS; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "This input describes the 10th joint only. hypot(a,active d) at seed divided by conservative extent.",
            "Candidate input for testing whether joint 10 active extent divided by reach helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For prismatic active d is the seed translation; for revolute d remains static. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(380,
            "joint_10_dh_alpha_sin",
            "Joint 10 sine of dh twist",
            "Per-joint transforms",
            "Ordered joint 10: Sine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; sin(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 10th joint only. Sine of this row static alpha angle.",
            "Candidate input for testing whether joint 10 sine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Dimensionless periodic encoding; absent slot returns0. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(381,
            "joint_10_dh_alpha_cos",
            "Joint 10 cosine of dh twist",
            "Per-joint transforms",
            "Ordered joint 10: Cosine of this row static alpha angle.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; cos(input.alpha[index]); ",
            "dimensionless",
            listOf("dhAlphaRad","jointCount"),
            "This input describes the 10th joint only. Cosine of this row static alpha angle.",
            "Candidate input for testing whether joint 10 cosine of dh twist helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "For a present zero twist returns1, while absent slot returns0. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[12]",
            "feature-dictionary-v1"),
        FeatureDefinition(382,
            "joint_10_active_theta_sin",
            "Joint 10 sine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 10: Sine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; sin(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Sine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 10 sine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "It does not apply trigonometry to a prismatic coordinate. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(383,
            "joint_10_active_theta_cos",
            "Joint 10 cosine of active dh theta",
            "Per-joint transforms",
            "Ordered joint 10: Cosine of seed rotation for revolute or static theta for prismatic.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; cos(activeTheta); activeTheta = seeds[index] if revolute else theta[index]; revolute = jointTypes[index] is REVOLUTE",
            "dimensionless",
            listOf("dhThetaRad","jointTypes","seedJointValues","jointCount"),
            "This input describes the 10th joint only. Cosine of seed rotation for revolute or static theta for prismatic.",
            "Candidate input for testing whether joint 10 cosine of active dh theta helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Present zero angle yields1; all absent slots, including cosine, yield0. Present only when jointCount≥10; otherwise exactly0 in all15 expanded slots. Check baseline joint_10_present. Positive span required; half span floor1e-12. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ExpandedContextFeatureCalculator.kt:calculate · JOINT_FEATURE_SUFFIXES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(384,
            "research_v2_workspace_outer_pressure_squared",
            "Research v2 workspace outer pressure squared",
            "Research v2 nonlinear global context",
            "Squared target-radius utilization of the seed-posture extent sum.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "outerPressure * outerPressure; outerPressure = max(0,utilization); utilization = targetRadius/reach; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Squared target-radius utilization of the seed-posture extent sum.",
            "Candidate input for testing whether research v2 workspace outer pressure squared helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[0]",
            "feature-dictionary-v1")
    )
    private fun block32():List<FeatureDefinition> = listOf(
        FeatureDefinition(385,
            "research_v2_workspace_outer_pressure_fourth",
            "Research v2 workspace outer pressure fourth",
            "Research v2 nonlinear global context",
            "Fourth power of target-radius utilization, emphasizing extreme numeric radial utilization.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "square(square(outerPressure)); square = value*value; outerPressure = max(0,utilization); utilization = targetRadius/reach; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Fourth power of target-radius utilization, emphasizing extreme numeric radial utilization.",
            "Candidate input for testing whether research v2 workspace outer pressure fourth helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(386,
            "research_v2_workspace_inner_pressure_squared",
            "Research v2 workspace inner pressure squared",
            "Research v2 nonlinear global context",
            "Squared normalized penetration inside the seed-posture triangle inner-void proxy.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "innerPressure * innerPressure; innerPressure = max(0,(innerBound-targetRadius)/reach); innerBound = max(0,maximumExtent-(reach-maximumExtent)); maximumExtent = max(extents); extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z)",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Squared normalized penetration inside the seed-posture triangle inner-void proxy.",
            "Candidate input for testing whether research v2 workspace inner pressure squared helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(387,
            "research_v2_workspace_boundary_inverse_distance",
            "Research v2 workspace boundary inverse distance",
            "Research v2 nonlinear global context",
            "Reciprocal normalized distance to the nearest inner/outer radial proxy, floored to remain finite.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "1.0 / max(nearestBoundaryDistance, MARGIN_FLOOR); nearestBoundaryDistance = min(abs(targetRadius-innerBound),abs(reach-targetRadius))/reach; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z); innerBound = max(0,maximumExtent-(reach-maximumExtent)); maximumExtent = max(extents); extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Reciprocal normalized distance to the nearest inner/outer radial proxy, floored to remain finite.",
            "Candidate input for testing whether research v2 workspace boundary inverse distance helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(388,
            "research_v2_workspace_boundary_within_05",
            "Research v2 workspace boundary within 05",
            "Research v2 nonlinear global context",
            "Indicator that normalized radial distance to either proxy is at most0.05.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "if (nearestBoundaryDistance <= 0.05) 1.0 else 0.0; nearestBoundaryDistance = min(abs(targetRadius-innerBound),abs(reach-targetRadius))/reach; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z); innerBound = max(0,maximumExtent-(reach-maximumExtent)); maximumExtent = max(extents); extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Indicator that normalized radial distance to either proxy is at most0.05.",
            "Candidate input for testing whether research v2 workspace boundary within 05 helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(389,
            "research_v2_workspace_boundary_within_10",
            "Research v2 workspace boundary within 10",
            "Research v2 nonlinear global context",
            "Indicator that normalized radial distance to either proxy is at most0.10.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "if (nearestBoundaryDistance <= 0.10) 1.0 else 0.0; nearestBoundaryDistance = min(abs(targetRadius-innerBound),abs(reach-targetRadius))/reach; targetRadius = norm3(targetX,targetY,targetZ); norm3 = hypot(hypot(x,y),z); innerBound = max(0,maximumExtent-(reach-maximumExtent)); maximumExtent = max(extents); extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointTypes","seedJointValues","targetX","targetY","targetZ"),
            "Indicator that normalized radial distance to either proxy is at most0.10.",
            "Candidate input for testing whether research v2 workspace boundary within 10 helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(390,
            "research_v2_joint_pressure_squared_mean",
            "Research v2 joint pressure squared mean",
            "Research v2 nonlinear global context",
            "Mean squared absolute centre-relative joint pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "pressureSquares.average(); pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Mean squared absolute centre-relative joint pressure.",
            "Candidate input for testing whether research v2 joint pressure squared mean helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(391,
            "research_v2_joint_pressure_fourth_mean",
            "Research v2 joint pressure fourth mean",
            "Research v2 nonlinear global context",
            "Mean fourth-power centre-relative joint pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "pressureFourths.average(); pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Mean fourth-power centre-relative joint pressure.",
            "Candidate input for testing whether research v2 joint pressure fourth mean helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[7]",
            "feature-dictionary-v1"),
        FeatureDefinition(392,
            "research_v2_joint_pressure_squared_rms",
            "Research v2 joint pressure squared rms",
            "Research v2 nonlinear global context",
            "RMS of squared pressure: square root of mean pressure fourth powers.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "sqrt(pressureSquares.map(::square).average()); pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; square = value*value",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "RMS of squared pressure: square root of mean pressure fourth powers.",
            "Candidate input for testing whether research v2 joint pressure squared rms helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[8]",
            "feature-dictionary-v1"),
        FeatureDefinition(393,
            "research_v2_joint_pressure_dispersion",
            "Research v2 joint pressure dispersion",
            "Research v2 nonlinear global context",
            "Population standard deviation of absolute joint pressures.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "standardDeviation(pressure); standardDeviation = population standard deviation: stableNorm(values-mean)/sqrt(number of values);0 if fewer than2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues"),
            "Population standard deviation of absolute joint pressures.",
            "Candidate input for testing whether research v2 joint pressure dispersion helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[9]",
            "feature-dictionary-v1"),
        FeatureDefinition(394,
            "research_v2_critical_joint_fraction_02",
            "Research v2 critical joint fraction 02",
            "Research v2 nonlinear global context",
            "Fraction of joints whose nearest margin is at most0.02 of full span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "margins.count { it <= 0.02 }.toDouble() / count; margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; count = jointTypes.size",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Fraction of joints whose nearest margin is at most0.02 of full span.",
            "Candidate input for testing whether research v2 critical joint fraction 02 helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[10]",
            "feature-dictionary-v1"),
        FeatureDefinition(395,
            "research_v2_critical_joint_fraction_05",
            "Research v2 critical joint fraction 05",
            "Research v2 nonlinear global context",
            "Fraction of joints whose nearest margin is at most0.05 of full span.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "margins.count { it <= 0.05 }.toDouble() / count; margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; count = jointTypes.size",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","jointTypes","seedJointValues"),
            "Fraction of joints whose nearest margin is at most0.05 of full span.",
            "Candidate input for testing whether research v2 critical joint fraction 05 helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[11]",
            "feature-dictionary-v1"),
        FeatureDefinition(396,
            "research_v2_dominant_link_fraction_squared",
            "Research v2 dominant link fraction squared",
            "Research v2 nonlinear global context",
            "Squared share of the largest seed-posture active extent.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "dominantLink * dominantLink; dominantLink = maximumExtent/reach; maximumExtent = max(extents); extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointTypes","seedJointValues"),
            "Squared share of the largest seed-posture active extent.",
            "Candidate input for testing whether research v2 dominant link fraction squared helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[12]",
            "feature-dictionary-v1")
    )
    private fun block33():List<FeatureDefinition> = listOf(
        FeatureDefinition(397,
            "research_v2_inner_void_fraction_squared",
            "Research v2 inner void fraction squared",
            "Research v2 nonlinear global context",
            "Squared fraction of seed-posture extent occupied by the inner-void proxy.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "innerVoidFraction * innerVoidFraction; innerVoidFraction = innerBound/reach; innerBound = max(0,maximumExtent-(reach-maximumExtent)); maximumExtent = max(extents); extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointTypes","seedJointValues"),
            "Squared fraction of seed-posture extent occupied by the inner-void proxy.",
            "Candidate input for testing whether research v2 inner void fraction squared helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[13]",
            "feature-dictionary-v1"),
        FeatureDefinition(398,
            "research_v2_link_extent_gini",
            "Research v2 link extent gini",
            "Research v2 nonlinear global context",
            "Gini inequality of seed-posture active extents over present joints.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "gini(extents); gini = sum_i sum_j abs(extents[i]-extents[j])/(2*count^2*mean(extents));0 if mean<=EPS or empty; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); count = jointTypes.size; EPS = 1e-12",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointTypes","seedJointValues"),
            "Gini inequality of seed-posture active extents over present joints.",
            "Candidate input for testing whether research v2 link extent gini helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Experimental pre-solve transform, not a proven improvement. Research reach uses current seed prismatic displacement, unlike the conservative bound of expanded383. Radial boundaries are proxies, not membership certificates. pressure=abs(centre offset/halfspan) clipped≤1; inverse margin floor1e-4, so reciprocal≤10000. No solver outcome, actual iteration count or final error is used. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · GLOBAL_NAMES[14]",
            "feature-dictionary-v1"),
        FeatureDefinition(399,
            "research_v2_joint_1_limit_pressure_squared",
            "Joint 1 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 1: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 1 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 1 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<1, all7 research slots for this joint are exactly0; interpret using joint_1_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(400,
            "research_v2_joint_1_limit_pressure_fourth",
            "Joint 1 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 1: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 1 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 1 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<1, all7 research slots for this joint are exactly0; interpret using joint_1_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(401,
            "research_v2_joint_1_inverse_nearest_margin",
            "Joint 1 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 1: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 1 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 1 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<1, all7 research slots for this joint are exactly0; interpret using joint_1_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(402,
            "research_v2_joint_1_limit_exponential_pressure",
            "Joint 1 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 1: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 1 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 1 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<1, all7 research slots for this joint are exactly0; interpret using joint_1_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(403,
            "research_v2_joint_1_home_displacement_squared",
            "Joint 1 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 1: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 1 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 1 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<1, all7 research slots for this joint are exactly0; interpret using joint_1_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(404,
            "research_v2_joint_1_home_displacement_x_limit_pressure",
            "Joint 1 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 1: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 1 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 1 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<1, all7 research slots for this joint are exactly0; interpret using joint_1_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(405,
            "research_v2_joint_1_link_leverage_x_limit_pressure",
            "Joint 1 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 1: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 0; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 1 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 1 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<1, all7 research slots for this joint are exactly0; interpret using joint_1_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(406,
            "research_v2_joint_2_limit_pressure_squared",
            "Joint 2 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 2: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 2 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 2 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<2, all7 research slots for this joint are exactly0; interpret using joint_2_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(407,
            "research_v2_joint_2_limit_pressure_fourth",
            "Joint 2 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 2: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 2 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 2 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<2, all7 research slots for this joint are exactly0; interpret using joint_2_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(408,
            "research_v2_joint_2_inverse_nearest_margin",
            "Joint 2 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 2: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 2 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 2 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<2, all7 research slots for this joint are exactly0; interpret using joint_2_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1")
    )
    private fun block34():List<FeatureDefinition> = listOf(
        FeatureDefinition(409,
            "research_v2_joint_2_limit_exponential_pressure",
            "Joint 2 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 2: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 2 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 2 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<2, all7 research slots for this joint are exactly0; interpret using joint_2_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(410,
            "research_v2_joint_2_home_displacement_squared",
            "Joint 2 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 2: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 2 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 2 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<2, all7 research slots for this joint are exactly0; interpret using joint_2_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(411,
            "research_v2_joint_2_home_displacement_x_limit_pressure",
            "Joint 2 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 2: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 2 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 2 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<2, all7 research slots for this joint are exactly0; interpret using joint_2_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(412,
            "research_v2_joint_2_link_leverage_x_limit_pressure",
            "Joint 2 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 2: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 1; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 2 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 2 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<2, all7 research slots for this joint are exactly0; interpret using joint_2_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(413,
            "research_v2_joint_3_limit_pressure_squared",
            "Joint 3 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 3: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 3 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 3 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<3, all7 research slots for this joint are exactly0; interpret using joint_3_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(414,
            "research_v2_joint_3_limit_pressure_fourth",
            "Joint 3 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 3: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 3 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 3 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<3, all7 research slots for this joint are exactly0; interpret using joint_3_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(415,
            "research_v2_joint_3_inverse_nearest_margin",
            "Joint 3 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 3: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 3 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 3 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<3, all7 research slots for this joint are exactly0; interpret using joint_3_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(416,
            "research_v2_joint_3_limit_exponential_pressure",
            "Joint 3 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 3: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 3 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 3 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<3, all7 research slots for this joint are exactly0; interpret using joint_3_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(417,
            "research_v2_joint_3_home_displacement_squared",
            "Joint 3 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 3: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 3 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 3 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<3, all7 research slots for this joint are exactly0; interpret using joint_3_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(418,
            "research_v2_joint_3_home_displacement_x_limit_pressure",
            "Joint 3 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 3: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 3 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 3 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<3, all7 research slots for this joint are exactly0; interpret using joint_3_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(419,
            "research_v2_joint_3_link_leverage_x_limit_pressure",
            "Joint 3 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 3: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 2; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 3 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 3 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<3, all7 research slots for this joint are exactly0; interpret using joint_3_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(420,
            "research_v2_joint_4_limit_pressure_squared",
            "Joint 4 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 4: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 4 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 4 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<4, all7 research slots for this joint are exactly0; interpret using joint_4_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1")
    )
    private fun block35():List<FeatureDefinition> = listOf(
        FeatureDefinition(421,
            "research_v2_joint_4_limit_pressure_fourth",
            "Joint 4 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 4: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 4 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 4 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<4, all7 research slots for this joint are exactly0; interpret using joint_4_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(422,
            "research_v2_joint_4_inverse_nearest_margin",
            "Joint 4 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 4: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 4 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 4 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<4, all7 research slots for this joint are exactly0; interpret using joint_4_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(423,
            "research_v2_joint_4_limit_exponential_pressure",
            "Joint 4 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 4: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 4 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 4 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<4, all7 research slots for this joint are exactly0; interpret using joint_4_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(424,
            "research_v2_joint_4_home_displacement_squared",
            "Joint 4 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 4: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 4 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 4 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<4, all7 research slots for this joint are exactly0; interpret using joint_4_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(425,
            "research_v2_joint_4_home_displacement_x_limit_pressure",
            "Joint 4 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 4: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 4 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 4 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<4, all7 research slots for this joint are exactly0; interpret using joint_4_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(426,
            "research_v2_joint_4_link_leverage_x_limit_pressure",
            "Joint 4 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 4: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 3; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 4 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 4 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<4, all7 research slots for this joint are exactly0; interpret using joint_4_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(427,
            "research_v2_joint_5_limit_pressure_squared",
            "Joint 5 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 5: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 5 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 5 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<5, all7 research slots for this joint are exactly0; interpret using joint_5_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(428,
            "research_v2_joint_5_limit_pressure_fourth",
            "Joint 5 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 5: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 5 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 5 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<5, all7 research slots for this joint are exactly0; interpret using joint_5_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(429,
            "research_v2_joint_5_inverse_nearest_margin",
            "Joint 5 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 5: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 5 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 5 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<5, all7 research slots for this joint are exactly0; interpret using joint_5_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(430,
            "research_v2_joint_5_limit_exponential_pressure",
            "Joint 5 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 5: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 5 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 5 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<5, all7 research slots for this joint are exactly0; interpret using joint_5_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(431,
            "research_v2_joint_5_home_displacement_squared",
            "Joint 5 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 5: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 5 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 5 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<5, all7 research slots for this joint are exactly0; interpret using joint_5_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(432,
            "research_v2_joint_5_home_displacement_x_limit_pressure",
            "Joint 5 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 5: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 5 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 5 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<5, all7 research slots for this joint are exactly0; interpret using joint_5_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1")
    )
    private fun block36():List<FeatureDefinition> = listOf(
        FeatureDefinition(433,
            "research_v2_joint_5_link_leverage_x_limit_pressure",
            "Joint 5 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 5: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 4; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 5 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 5 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<5, all7 research slots for this joint are exactly0; interpret using joint_5_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(434,
            "research_v2_joint_6_limit_pressure_squared",
            "Joint 6 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 6: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 6 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 6 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<6, all7 research slots for this joint are exactly0; interpret using joint_6_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(435,
            "research_v2_joint_6_limit_pressure_fourth",
            "Joint 6 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 6: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 6 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 6 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<6, all7 research slots for this joint are exactly0; interpret using joint_6_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(436,
            "research_v2_joint_6_inverse_nearest_margin",
            "Joint 6 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 6: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 6 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 6 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<6, all7 research slots for this joint are exactly0; interpret using joint_6_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(437,
            "research_v2_joint_6_limit_exponential_pressure",
            "Joint 6 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 6: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 6 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 6 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<6, all7 research slots for this joint are exactly0; interpret using joint_6_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(438,
            "research_v2_joint_6_home_displacement_squared",
            "Joint 6 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 6: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 6 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 6 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<6, all7 research slots for this joint are exactly0; interpret using joint_6_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(439,
            "research_v2_joint_6_home_displacement_x_limit_pressure",
            "Joint 6 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 6: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 6 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 6 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<6, all7 research slots for this joint are exactly0; interpret using joint_6_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(440,
            "research_v2_joint_6_link_leverage_x_limit_pressure",
            "Joint 6 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 6: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 5; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 6 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 6 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<6, all7 research slots for this joint are exactly0; interpret using joint_6_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(441,
            "research_v2_joint_7_limit_pressure_squared",
            "Joint 7 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 7: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 7 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 7 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<7, all7 research slots for this joint are exactly0; interpret using joint_7_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(442,
            "research_v2_joint_7_limit_pressure_fourth",
            "Joint 7 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 7: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 7 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 7 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<7, all7 research slots for this joint are exactly0; interpret using joint_7_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(443,
            "research_v2_joint_7_inverse_nearest_margin",
            "Joint 7 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 7: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 7 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 7 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<7, all7 research slots for this joint are exactly0; interpret using joint_7_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(444,
            "research_v2_joint_7_limit_exponential_pressure",
            "Joint 7 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 7: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 7 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 7 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<7, all7 research slots for this joint are exactly0; interpret using joint_7_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1")
    )
    private fun block37():List<FeatureDefinition> = listOf(
        FeatureDefinition(445,
            "research_v2_joint_7_home_displacement_squared",
            "Joint 7 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 7: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 7 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 7 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<7, all7 research slots for this joint are exactly0; interpret using joint_7_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(446,
            "research_v2_joint_7_home_displacement_x_limit_pressure",
            "Joint 7 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 7: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 7 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 7 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<7, all7 research slots for this joint are exactly0; interpret using joint_7_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(447,
            "research_v2_joint_7_link_leverage_x_limit_pressure",
            "Joint 7 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 7: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 6; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 7 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 7 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<7, all7 research slots for this joint are exactly0; interpret using joint_7_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(448,
            "research_v2_joint_8_limit_pressure_squared",
            "Joint 8 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 8: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 8 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 8 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<8, all7 research slots for this joint are exactly0; interpret using joint_8_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(449,
            "research_v2_joint_8_limit_pressure_fourth",
            "Joint 8 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 8: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 8 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 8 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<8, all7 research slots for this joint are exactly0; interpret using joint_8_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(450,
            "research_v2_joint_8_inverse_nearest_margin",
            "Joint 8 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 8: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 8 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 8 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<8, all7 research slots for this joint are exactly0; interpret using joint_8_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(451,
            "research_v2_joint_8_limit_exponential_pressure",
            "Joint 8 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 8: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 8 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 8 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<8, all7 research slots for this joint are exactly0; interpret using joint_8_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(452,
            "research_v2_joint_8_home_displacement_squared",
            "Joint 8 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 8: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 8 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 8 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<8, all7 research slots for this joint are exactly0; interpret using joint_8_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(453,
            "research_v2_joint_8_home_displacement_x_limit_pressure",
            "Joint 8 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 8: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 8 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 8 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<8, all7 research slots for this joint are exactly0; interpret using joint_8_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(454,
            "research_v2_joint_8_link_leverage_x_limit_pressure",
            "Joint 8 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 8: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 7; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 8 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 8 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<8, all7 research slots for this joint are exactly0; interpret using joint_8_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(455,
            "research_v2_joint_9_limit_pressure_squared",
            "Joint 9 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 9: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 9 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 9 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<9, all7 research slots for this joint are exactly0; interpret using joint_9_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(456,
            "research_v2_joint_9_limit_pressure_fourth",
            "Joint 9 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 9: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 9 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 9 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<9, all7 research slots for this joint are exactly0; interpret using joint_9_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1")
    )
    private fun block38():List<FeatureDefinition> = listOf(
        FeatureDefinition(457,
            "research_v2_joint_9_inverse_nearest_margin",
            "Joint 9 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 9: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 9 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 9 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<9, all7 research slots for this joint are exactly0; interpret using joint_9_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(458,
            "research_v2_joint_9_limit_exponential_pressure",
            "Joint 9 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 9: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 9 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 9 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<9, all7 research slots for this joint are exactly0; interpret using joint_9_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(459,
            "research_v2_joint_9_home_displacement_squared",
            "Joint 9 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 9: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 9 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 9 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<9, all7 research slots for this joint are exactly0; interpret using joint_9_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(460,
            "research_v2_joint_9_home_displacement_x_limit_pressure",
            "Joint 9 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 9: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 9 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 9 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<9, all7 research slots for this joint are exactly0; interpret using joint_9_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(461,
            "research_v2_joint_9_link_leverage_x_limit_pressure",
            "Joint 9 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 9: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 8; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 9 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 9 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<9, all7 research slots for this joint are exactly0; interpret using joint_9_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1"),
        FeatureDefinition(462,
            "research_v2_joint_10_limit_pressure_squared",
            "Joint 10 research squared limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 10: Square of absolute centre-relative position in half-span units.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; pressureSquared; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 10 in the declared serial order. Square of absolute centre-relative position in half-span units.",
            "Candidate input for testing whether joint 10 research squared limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Emphasizes high pressure regardless of lower/upper side. If jointCount<10, all7 research slots for this joint are exactly0; interpret using joint_10_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[0]",
            "feature-dictionary-v1"),
        FeatureDefinition(463,
            "research_v2_joint_10_limit_pressure_fourth",
            "Joint 10 research fourth-power limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 10: Fourth power of absolute centre-relative position.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; pressureFourths[index]; pressureFourths = pressure^4; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 10 in the declared serial order. Fourth power of absolute centre-relative position.",
            "Candidate input for testing whether joint 10 research fourth-power limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Compresses central pressures more than square; not a calibrated failure probability. If jointCount<10, all7 research slots for this joint are exactly0; interpret using joint_10_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[1]",
            "feature-dictionary-v1"),
        FeatureDefinition(464,
            "research_v2_joint_10_inverse_nearest_margin",
            "Joint 10 research inverse nearest margin",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 10: Reciprocal of nearest full-span margin floored at1e-4.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; 1.0 / max(margins[index], MARGIN_FLOOR); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; MARGIN_FLOOR = 1e-4",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 10 in the declared serial order. Reciprocal of nearest full-span margin floored at1e-4.",
            "Candidate input for testing whether joint 10 research inverse nearest margin helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Finite cap10000; at centre value2, at boundary cap10000. If jointCount<10, all7 research slots for this joint are exactly0; interpret using joint_10_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[2]",
            "feature-dictionary-v1"),
        FeatureDefinition(465,
            "research_v2_joint_10_limit_exponential_pressure",
            "Joint 10 research exponential limit pressure",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 10: exp(-10*nearest full-span margin).",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; exp(-10.0 * margins[index]); margins = (1-pressure[j])*0.5; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 10 in the declared serial order. exp(-10*nearest full-span margin).",
            "Candidate input for testing whether joint 10 research exponential limit pressure helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "1 at a boundary and exp(-5) at centre; not a physical energy or likelihood. If jointCount<10, all7 research slots for this joint are exactly0; interpret using joint_10_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[3]",
            "feature-dictionary-v1"),
        FeatureDefinition(466,
            "research_v2_joint_10_home_displacement_squared",
            "Joint 10 research squared home displacement",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 10: Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; displacementSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 10 in the declared serial order. Square of seed-home offset divided by half span, after clipping displacement to[-2,2].",
            "Candidate input for testing whether joint 10 research squared home displacement helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Maximum4 under valid limits; direction is discarded. If jointCount<10, all7 research slots for this joint are exactly0; interpret using joint_10_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[4]",
            "feature-dictionary-v1"),
        FeatureDefinition(467,
            "research_v2_joint_10_home_displacement_x_limit_pressure",
            "Joint 10 research home-displacement and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 10: Squared half-span home displacement multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; displacementSquared * pressureSquared; displacementSquared = homeDisplacement[index]^2; homeDisplacement = clamp((seeds[j]-homes[j])/halfSpans[j],-2,2); halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5",
            "dimensionless",
            listOf("jointHomeValues","jointMaxValues","jointMinValues","seedJointValues","jointCount"),
            "Specific to joint 10 in the declared serial order. Squared half-span home displacement multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 10 research home-displacement and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Product represents co-occurrence for this joint; it is not causal interaction evidence. If jointCount<10, all7 research slots for this joint are exactly0; interpret using joint_10_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[5]",
            "feature-dictionary-v1"),
        FeatureDefinition(468,
            "research_v2_joint_10_link_leverage_x_limit_pressure",
            "Joint 10 research active-extent and limit-pressure interaction",
            "Research v2 per-joint nonlinear context",
            "Ordered joint 10: This joint active extent share multiplied by squared limit pressure.",
            "Encoded before IK from scientific CSV inputs. Recomputed by standard-DH seed kinematics and mathematical transforms; not a stored output label.",
            "index = 9; extentShare * pressureSquared; extentShare = extents[index]/reach; extents = hypot(a[j],if jointTypes[j] PRISMATIC then seeds[j] else d[j]); reach = max(sum(extents),EPS); seed-posture extent sum, not the conservative reach based on prismatic limits; EPS = 1e-12; pressureSquared = pressureSquares[index]; pressureSquares = pressure^2; pressure = abs((seeds[j]-centers[j])/halfSpans[j]).coerceIn(-1,1); centers = (minimums[j] + maximums[j]) * 0.5; halfSpans = max(spans[j] * 0.5, EPS); spans = maximums[j] - minimums[j] for each present j",
            "dimensionless",
            listOf("dhAMeters","dhDMeters","jointMaxValues","jointMinValues","jointTypes","seedJointValues","jointCount"),
            "Specific to joint 10 in the declared serial order. This joint active extent share multiplied by squared limit pressure.",
            "Candidate input for testing whether joint 10 research active-extent and limit-pressure interaction helps held-out solver-outcome discrimination; incremental usefulness must be measured by paired ablation.",
            "Uses current prismatic extension in extent, not raw static d or maximal reach; no causal interpretation. If jointCount<10, all7 research slots for this joint are exactly0; interpret using joint_10_present. No features depend on solved coordinates. Margins and pressure derive from the same validated seed and limits. Only finite, dimensionally matched inputs with positive joint spans, valid joint types, seed/home inside limits and zero active DH fields are admitted. Encoder values are Float; overflow after conversion rejects the row. Classifier training normalization is a separate train-fitted operation, clipped to [-8,8], not this feature formula.",
            "app/src/main/java/com/robotkinematicslab/mobile/ml/data/ResearchContextFeatureCalculator.kt:calculate · JOINT_SUFFIXES[6]",
            "feature-dictionary-v1")
    )
}
